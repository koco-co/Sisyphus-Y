---
phase: 06
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [E2E-TEST-02, E2E-TEST-04]
user_setup:
  - service: Chrome DevTools MCP
    why: "端到端浏览器自动化测试"
    env_vars: []
    dashboard_config:
      - task: "确保 Chrome 浏览器已打开并可访问"
        location: "本地 Chrome 浏览器"
must_haves:
  truths:
    - "分析台页面可正常加载"
    - "需求详情正确渲染 Markdown 内容"
    - "需求详情中的图片可正常显示"
    - "工作台页面可正常访问"
    - "用例库页面可正常加载"
    - "主链路端到端流程无报错"
  artifacts:
    - path: "前端运行在 http://localhost:3000"
      provides: "可访问的 Web 应用"
    - path: "后端运行在 http://localhost:8000"
      provides: "可访问的 API 服务"
  key_links:
    - from: "浏览器"
      to: "http://localhost:3000"
      via: "Chrome MCP navigate"
      pattern: "localhost:3000"
---

<objective>
使用 Chrome DevTools MCP 对平台主链路进行端到端测试，验证「分析台 → 工作台 → 用例库」核心流程正常工作，确保 Markdown 渲染和图片显示功能正确。

Purpose: 验证 Phase 6 实现的完整性和主链路的稳定性
Output: E2E 测试验证报告
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-CONTEXT.md
@.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-RESEARCH.md
</context>

<interfaces>
<!-- Chrome DevTools MCP Tools -->

Available MCP tools for E2E testing:
- `mcp__chrome-devtools__navigate` — 导航到 URL
- `mcp__chrome-devtools__screenshot` — 截取屏幕截图
- `mcp__chrome-devtools__click` — 点击元素
- `mcp__chrome-devtools__type` — 输入文本
- `mcp__chrome-devtools__wait_for` — 等待元素出现
- `mcp__chrome-devtools__evaluate` — 执行 JavaScript
- `mcp__chrome-devtools__list_pages` — 列出所有页面

Test data:
- 需求文档: `/Users/poco/Documents/DTStack/XmindCase/DataAssets/Story/Story-15602.md`
- 图片文件: `/Users/poco/Documents/DTStack/XmindCase/Assets/img/Pasted image 20260316170939.png`

Note: Story-15602.md 包含:
- 3 张外链图片（蓝湖 lanhuapp.com）
- 1 张 Obsidian 语法本地图片 `![[Pasted image 20260316170939.png]]`
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 实现的功能：
    1. RequirementDetailTab 组件使用 ReactMarkdown 渲染需求内容
    2. /api/files/{bucket}/{path} 端点提供 MinIO 文件访问
  </what-built>
  <how-to-verify>
    ## 前置条件检查

    在执行 E2E 测试前，请确认以下服务已启动：

    ### 1. 启动后端服务
    ```bash
    cd /Users/poco/Projects/Sisyphus-case-platform/backend
    uv run uvicorn app.main:app --reload --port 8000
    ```

    ### 2. 启动前端服务
    ```bash
    cd /Users/poco/Projects/Sisyphus-case-platform/frontend
    bun dev
    ```

    ### 3. 确认服务可用
    - 后端健康检查: http://localhost:8000/health 应返回 `{"status": "ok"}`
    - 前端页面: http://localhost:3000 应显示登录页面或仪表盘

    ### 4. 准备测试数据

    如果需要测试图片显示功能，需要先将本地图片上传到 MinIO：

    ```bash
    # 使用 Python 脚本上传图片（可选）
    cd /Users/poco/Projects/Sisyphus-case-platform/backend
    uv run python -c "
    import asyncio
    from app.engine.uda.image_handler import archive_image

    async def upload():
        with open('/Users/poco/Documents/DTStack/XmindCase/Assets/img/Pasted image 20260316170939.png', 'rb') as f:
            path = await archive_image(f.read(), 'Pasted image 20260316170939.png', 'test-req')
            print(f'Uploaded to: {path}')

    asyncio.run(upload())
    "
    ```

    ## E2E 测试步骤

    ### Step 1: 打开分析台页面
    1. 在浏览器中访问 http://localhost:3000
    2. 登录（如需要）
    3. 点击顶部导航栏的「分析台」菜单
    4. 确认左侧显示需求列表

    ### Step 2: 验证 Markdown 渲染
    1. 在需求列表中选择任意需求
    2. 点击右侧「需求详情」Tab
    3. 检查需求内容是否正确渲染：
       - 标题显示为粗体大字
       - 列表显示为带圆点的列表项
       - 代码显示为带背景色的等宽字体
       - 链接显示为带下划线的品牌色文字

    ### Step 3: 验证图片显示
    1. 如果需求包含图片，检查图片是否正常显示
    2. 注意：外链图片（蓝湖）可能需要网络访问
    3. 本地图片需要先上传到 MinIO

    ### Step 4: 测试工作台
    1. 点击「进入工作台」按钮（如有）
    2. 确认步骤条显示「Step 1 确认测试点」
    3. 确认测试点列表正常显示

    ### Step 5: 测试用例库
    1. 点击顶部导航栏的「用例库」菜单
    2. 确认左侧目录树正常显示
    3. 确认右侧用例列表正常显示

    ## 预期结果
    - 所有页面正常加载，无白屏或报错
    - Markdown 内容正确渲染（标题、列表、链接、代码）
    - 图片正常显示（如已上传到 MinIO）
    - 主链路流程顺畅，无阻塞问题
  </how-to-verify>
  <resume-signal>输入 "approved" 继续执行下一阶段，或描述发现的问题</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Chrome DevTools MCP 端到端测试
  </what-built>
  <action>
    ## 使用 Chrome DevTools MCP 执行自动化测试

    如果您希望使用 Claude 的 Chrome DevTools MCP 进行自动化测试，请按以下步骤操作：

    ### 1. 确保 Chrome 浏览器已打开

    ### 2. Claude 将使用以下 MCP 工具执行测试：
    - `mcp__chrome-devtools__navigate` — 导航到页面
    - `mcp__chrome-devtools__screenshot` — 截图验证
    - `mcp__chrome-devtools__evaluate` — 执行 JS 断言

    ### 3. 测试流程
    Claude 将：
    1. 导航到 http://localhost:3000
    2. 截图验证页面加载状态
    3. 执行 JavaScript 检查 DOM 结构
    4. 验证 Markdown 渲染元素

    ### 前置要求
    - Chrome 浏览器已打开
    - 前端服务运行在 localhost:3000
    - 后端服务运行在 localhost:8000
  </action>
  <resume-signal>输入 "chrome-ready" 启动 Chrome MCP 自动化测试，或输入 "skip-chrome" 跳过自动化测试</resume-signal>
</task>

</tasks>

<verification>
- 人工验证：检查页面渲染和交互
- Chrome MCP 验证（可选）：自动化截图和 DOM 检查
</verification>

<success_criteria>
- 分析台页面正常加载
- 需求详情正确渲染 Markdown 格式
- 图片可正常显示（如已上传）
- 工作台页面可访问
- 用例库页面正常显示
- 主链路无 JavaScript 报错
</success_criteria>

<output>
After completion, create `.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-03-SUMMARY.md`
</output>
