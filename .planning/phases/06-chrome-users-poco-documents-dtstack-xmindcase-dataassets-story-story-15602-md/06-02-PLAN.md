---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - backend/app/modules/files/__init__.py
  - backend/app/modules/files/router.py
  - backend/app/main.py
autonomous: true
requirements: [E2E-TEST-03]
user_setup: []
must_haves:
  truths:
    - "访问 /api/files/{bucket}/{path} 返回 302 重定向到 MinIO presigned URL"
    - "无效路径返回 404 错误"
    - "前端可以通过 /api/files/ 端点访问 MinIO 归档的图片"
  artifacts:
    - path: "backend/app/modules/files/router.py"
      provides: "文件访问 API 端点"
      exports: ["router"]
    - path: "backend/app/main.py"
      provides: "模块注册"
      contains: '"files"'
  key_links:
    - from: "router.py"
      to: "image_handler.get_image_url"
      via: "import and call"
      pattern: "from app.engine.uda.image_handler import get_image_url"
---

<objective>
创建 /api/files/{bucket}/{path:path} 端点，代理 MinIO 文件访问，返回 presigned URL 重定向。

Purpose: image_handler.py 将图片 URL 替换为 /api/files/ 前缀，需要对应端点返回实际文件
Output: 可访问的文件 API 端点
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-CONTEXT.md
@.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces from codebase -->

From backend/app/engine/uda/image_handler.py:
```python
def get_image_url(object_path: str, expires_hours: int = 24) -> str:
    """生成临时访问 URL"""
    from datetime import timedelta
    client = get_minio_client()
    bucket, _, key = object_path.partition("/")
    return client.presigned_get_object(bucket, key, expires=timedelta(hours=expires_hours))
```

From backend/app/main.py (第 16-38 行):
```python
_MODULE_NAMES = [
    "auth",
    "products",
    "uda",
    # ... 其他模块
]

def _collect_routers() -> list:
    """Import each module router with error handling for modules not yet ready."""
    routers = []
    for name in _MODULE_NAMES:
        module_path = f"app.modules.{name}.router"
        try:
            mod = importlib.import_module(module_path)
            routers.append(mod.router)
        except Exception:
            logger.warning("Failed to load router from %s — skipping", module_path, exc_info=True)
    return routers
```

From backend/app/engine/uda/image_handler.py (第 83-84 行):
```python
# URL 替换模式: 外链图片被替换为:
new_url = f"/api/files/{archived_path}"
# archived_path 格式: "sisyphus-images/images/{req_id}/{hash}{ext}"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 files 模块和 API 端点</name>
  <files>backend/app/modules/files/__init__.py, backend/app/modules/files/router.py, backend/app/main.py</files>
  <read_first>
    - backend/app/engine/uda/image_handler.py
    - backend/app/main.py
    - backend/tests/unit/test_files/test_router.py
    - backend/app/modules/products/router.py (作为参考)
  </read_first>
  <behavior>
    - Test 1: GET /api/files/sisyphus-images/images/test.png 返回 302 重定向
    - Test 2: GET /api/files/invalid 返回 404
  </behavior>
  <action>
    创建 files 模块:

    1. 创建 `backend/app/modules/files/__init__.py`（空文件）

    2. 创建 `backend/app/modules/files/router.py`:
       ```python
       """Files API — MinIO 文件代理访问端点"""

       from fastapi import APIRouter, HTTPException
       from fastapi.responses import RedirectResponse

       from app.engine.uda.image_handler import get_image_url

       router = APIRouter(prefix="/files", tags=["files"])


       @router.get("/{bucket}/{path:path}")
       async def serve_file(bucket: str, path: str):
           """
           代理 MinIO 文件访问，返回 presigned URL 重定向。

           Args:
               bucket: MinIO bucket 名称（如 sisyphus-images）
               path: 对象路径（如 images/req-id/hash.png）

           Returns:
               302 重定向到 presigned URL

           Raises:
               404: 文件不存在或无法生成 URL
           """
           object_path = f"{bucket}/{path}"
           try:
               url = get_image_url(object_path, expires_hours=24)
               return RedirectResponse(url=url, status_code=302)
           except Exception as e:
               raise HTTPException(status_code=404, detail=f"File not found: {e!s}")
       ```

    3. 修改 `backend/app/main.py`:
       在 `_MODULE_NAMES` 列表中添加 `"files"`:
       ```python
       _MODULE_NAMES = [
           "auth",
           "products",
           "uda",
           "import_clean",
           "diagnosis",
           "scene_map",
           "generation",
           "testcases",
           "diff",
           "coverage",
           "templates",
           "knowledge",
           "export",
           "execution",
           "analytics",
           "search",
           "dashboard",
           "audit",
           "recycle",
           "ai_config",
           "tasks",
           "files",  # 新增
       ]
       ```

    4. 确保 router 使用 `/files` 前缀，最终 URL 为 `/api/files/{bucket}/{path:path}`
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_files/ -v</automated>
  </verify>
  <done>
    - `backend/app/modules/files/router.py` 存在
    - `router.py` 包含 `@router.get("/{bucket}/{path:path}")` 装饰器
    - `router.py` 包含 `from app.engine.uda.image_handler import get_image_url`
    - `main.py` 的 `_MODULE_NAMES` 包含 `"files"`
    - 后端测试全部通过（GREEN）
  </done>
</task>

</tasks>

<verification>
- 运行后端测试：`cd backend && uv run pytest tests/unit/test_files/ -v`
- 测试输出显示所有测试通过
</verification>

<success_criteria>
- /api/files/{bucket}/{path} 端点可访问
- 有效路径返回 302 重定向到 presigned URL
- 无效路径返回 404 错误
- 后端单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-chrome-users-poco-documents-dtstack-xmindcase-dataassets-story-story-15602-md/06-02-SUMMARY.md`
</output>
