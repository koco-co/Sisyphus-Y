---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "09"
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(main)/requirements/_components/EditorToolbar.tsx
  - frontend/src/app/(main)/requirements/_components/PublishVersionDialog.tsx
  - frontend/src/app/(main)/diff/_components/DiffView.tsx
  - frontend/src/app/(main)/diff/_components/AffectedCases.tsx
  - backend/app/modules/products/router.py
  - backend/app/modules/products/service.py
autonomous: true
requirements:
  - DIF-01
  - DIF-02
  - DIF-03
  - DIF-04
  - DIF-05

must_haves:
  truths:
    - "需求编辑器右上角有「发布新版本」按钮，点击弹对话框填写版本说明后触发 Diff"
    - "DiffView 展示左旧右新并排视图：删除红色/新增绿色/修改黄色，支持折叠未变更段落"
    - "Diff 后受影响用例显示 Badge（needs_rewrite/needs_review/not_affected）"
    - "「一键推送到工作台」按钮调用 push-to-workbench API，Toast 显示推送数量"
    - "POST /products/requirements/{id}/publish-version 端点存在，返回 version_from/version_to"
  artifacts:
    - path: "frontend/src/app/(main)/requirements/_components/PublishVersionDialog.tsx"
      provides: "发布新版本对话框（版本说明 + 确认）"
    - path: "frontend/src/app/(main)/requirements/_components/EditorToolbar.tsx"
      provides: "含「发布新版本」按钮的编辑器工具栏"
      contains: "PublishVersionDialog"
    - path: "frontend/src/app/(main)/diff/_components/DiffView.tsx"
      provides: "并排 Diff 视图（side-by-side）"
      contains: "parseDiff"
    - path: "frontend/src/app/(main)/diff/_components/AffectedCases.tsx"
      provides: "受影响用例列表 + 推送工作台按钮"
      contains: "push-to-workbench"
    - path: "backend/app/modules/products/router.py"
      provides: "/requirements/{id}/publish-version 端点"
  key_links:
    - from: "PublishVersionDialog 确认"
      to: "POST /products/requirements/{id}/publish-version"
      via: "fetch → 返回 {version_from, version_to} → 跳转到 Diff 页"
      pattern: "publish-version"
    - from: "AffectedCases.tsx「一键推送」按钮"
      to: "POST /diff/{req_id}/push-to-workbench"
      via: "fetch POST → toast.success"
      pattern: "push-to-workbench"
---

<objective>
实现 Diff 完整链路：发布新版本入口、并排 Diff 视图、受影响用例打标与推送工作台。

Purpose: 实现 DIF-01~05，让需求变更触发自动影响分析，测试工程师可一键将需重写用例推送到工作台。
Output: 后端 publish-version 端点 + 前端并排 DiffView + 推送按钮。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-02-SUMMARY.md

<interfaces>
<!-- 后端 publish-version 端点设计 -->
```python
# backend/app/modules/products/service.py 新增方法（或已有 RequirementService）
async def publish_version(self, req_id: UUID, version_note: str | None) -> tuple[int, int]:
    """
    1. 读取当前 Requirement.version
    2. 创建 RequirementVersion 快照（snapshot=当前内容，version=current，note=version_note）
    3. req.version += 1
    4. commit
    5. 触发 DiffService.compute_diff(req_id, old_version, new_version)（异步，用 BackgroundTasks）
    6. return (old_version, new_version)
    """

# backend/app/modules/products/router.py 新增端点
@router.post("/requirements/{req_id}/publish-version")
async def publish_version(req_id: UUID, data: PublishVersionRequest, session: AsyncSessionDep, background_tasks: BackgroundTasks) -> dict:
    # PublishVersionRequest: version_note: str | None
    # return: {requirement_id, version_from, version_to}
```

<!-- 前端 DiffView 并排解析逻辑（来自 RESEARCH.md Pattern 5） -->
```typescript
type DiffLine = {
  type: 'add' | 'del' | 'ctx' | 'hunk';
  left?: string;   // 旧版内容（del + ctx）
  right?: string;  // 新版内容（add + ctx）
  lineNumLeft?: number;
  lineNumRight?: number;
};

function parseSideBySide(unifiedDiff: string): DiffLine[] {
  // 解析 unified diff 文本，生成左右对齐的行对
}
// 渲染：
// del 行：bg-sy-danger/10（左列），右列空白
// add 行：bg-sy-accent/10（右列），左列空白
// ctx 行：bg-transparent（双列相同）
// 修改（相邻 del+add）：左=bg-sy-warn/10，右=bg-sy-warn/10
```

<!-- 已有 DiffView.tsx 概要（180行，渲染 unified diff 原始文本） -->
当前 DiffView.tsx 显示原始 unified diff 文本，需升级为并排视图。
已有组件：AffectedCases.tsx（含 RegenerateButton），SemanticAnalysis.tsx，VersionSelector.tsx。

change_impact 值域（来自 04-02 Task 2）：
- "needs_rewrite" → 红色 Badge「需重写」
- "needs_review" → 黄色 Badge「需复核」
- "not_affected" → 绿色 Badge「不受影响」
- null → 灰色 Badge「未分析」
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 发布新版本后端端点 + 前端入口（DIF-01）</name>
  <files>
    backend/app/modules/products/router.py,
    backend/app/modules/products/service.py,
    frontend/src/app/(main)/requirements/_components/PublishVersionDialog.tsx,
    frontend/src/app/(main)/requirements/_components/EditorToolbar.tsx
  </files>
  <action>
    后端部分：
    先读取 products/service.py，找到 RequirementService 或相关 service。
    添加 publish_version(req_id, version_note) 方法：
    1. SELECT Requirement WHERE id=req_id AND deleted_at IS NULL
    2. old_version = req.version
    3. INSERT RequirementVersion(requirement_id=req_id, version=old_version, snapshot={title,content,status}, note=version_note)
    4. req.version = old_version + 1
    5. await session.commit()
    6. 用 BackgroundTasks 异步触发 DiffService.compute_diff（不阻塞请求）
    7. return (old_version, old_version + 1)

    在 router.py 添加 POST /requirements/{req_id}/publish-version 端点。
    PublishVersionRequest schema 在 products/schemas.py 中添加（version_note: str | None = None）。

    前端部分：
    创建 PublishVersionDialog.tsx：
    - Props: {open, reqId, onClose, onSuccess: (versionFrom: number, versionTo: number) => void}
    - 表单：Textarea「版本说明（可选）」+ 「确认发布」按钮
    - 调用 POST /products/requirements/{reqId}/publish-version
    - 成功后调用 onSuccess(version_from, version_to)

    修改 EditorToolbar.tsx（先阅读，找到合适位置）：
    - 添加「发布新版本」Button（icon: Tag from lucide-react，variant="outline"，size="sm"）
    - 按钮 onClick → setPublishOpen(true)（state）
    - 渲染 PublishVersionDialog，onSuccess 时跳转到 /diff?req_id={reqId}
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_diff/ -k "publish" -v 2>&1 | tail -10 && cd ../frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*(EditorToolbar|PublishVersion)" | head -5</automated>
  </verify>
  <done>
    POST /requirements/{id}/publish-version 端点存在，EditorToolbar 有「发布新版本」按钮，PublishVersionDialog 组件 TypeScript 无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 并排 DiffView + 受影响用例 Badge + 推送工作台（DIF-02, DIF-03, DIF-04, DIF-05）</name>
  <files>
    frontend/src/app/(main)/diff/_components/DiffView.tsx,
    frontend/src/app/(main)/diff/_components/AffectedCases.tsx
  </files>
  <action>
    先阅读 DiffView.tsx（当前180行）和 AffectedCases.tsx 了解现有结构。

    升级 DiffView.tsx 为并排视图（DIF-02）：
    1. 添加 parseSideBySide(unifiedDiff: string) 纯函数，将 unified diff 解析为行对数组
    2. 渲染：两列等宽 grid（grid-cols-2），左列显示旧版（del+ctx），右列显示新版（add+ctx）
    3. 行颜色：
       - del 行左列：bg-sy-danger/10 border-l-2 border-sy-danger
       - add 行右列：bg-sy-accent/10 border-l-2 border-sy-accent
       - ctx 行双列：transparent
    4. 折叠功能（DIF-02「折叠未变更段落」）：超过3行连续 ctx 行时，显示折叠按钮「展开 N 行未变更内容」，默认折叠
    5. 保留已有 Tab 结构（文本对比 Tab + 变更摘要 Tab），仅替换文本对比 Tab 内容

    更新 AffectedCases.tsx（DIF-04, DIF-05）：
    1. 每条受影响用例旁显示 change_impact Badge：
       - needs_rewrite：红色（bg-sy-danger/10 text-sy-danger border-sy-danger/35）「需重写」
       - needs_review：黄色（bg-sy-warn/10 text-sy-warn border-sy-warn/35）「需复核」
       - not_affected：绿色（bg-sy-accent/10 text-sy-accent border-sy-accent/35）「不受影响」
       - null：灰色「未分析」
    2. 「一键推送到工作台」按钮（只推送 needs_rewrite 用例）：
       - 只有存在 needs_rewrite 用例时按钮激活
       - 点击调用 POST /diff/{reqId}/push-to-workbench
       - 成功后 toast.success(`已推送 ${result.pushed_count} 条用例到工作台`)
       - 按钮 loading 状态（Loader2 animate-spin）
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*(DiffView|AffectedCases)" | head -10</automated>
  </verify>
  <done>
    DiffView.tsx 有 parseSideBySide 函数和两列并排渲染，AffectedCases.tsx 有 change_impact Badge 和「一键推送」按钮，TypeScript 无错误
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run ruff check app/modules/products/ && uv run pyright app/modules/products/ --level warning 2>&1 | grep "error:" | head -5 && cd ../frontend && bunx tsc --noEmit 2>&1 | grep -c "error TS"
```
ruff 无 lint 错误，tsc 错误数为 0。
</verification>

<success_criteria>
- POST /products/requirements/{id}/publish-version 端点存在，返回 version_from/version_to
- EditorToolbar.tsx 有「发布新版本」按钮触发 PublishVersionDialog
- DiffView.tsx 并排视图：del红/add绿/未变更折叠
- AffectedCases.tsx 有 change_impact Badge（needs_rewrite/needs_review/not_affected）
- 「一键推送到工作台」按钮调用正确 API，Toast 显示推送数量
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-09-SUMMARY.md`
</output>
