---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "06"
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(main)/testcases/_components/ImportDialog.tsx
  - frontend/src/app/(main)/testcases/_components/import/FormatSelectStep.tsx
  - frontend/src/app/(main)/testcases/_components/import/FieldMappingStep.tsx
  - frontend/src/app/(main)/testcases/_components/import/DuplicateStep.tsx
  - frontend/src/app/(main)/testcases/_components/import/ResultStep.tsx
autonomous: true
requirements:
  - TC-07
  - TC-08
  - TC-09
  - TC-10
  - TC-11

must_haves:
  truths:
    - "ImportDialog 拆分为至少4个子组件，主文件行数 ≤ 400 行"
    - "格式选择步骤旁有「下载模板」按钮（4种格式模板文件）"
    - "字段映射步骤：必填字段（title）未映射时「下一步」按钮禁用"
    - "重复检测步骤：列出重复条目，用户可选择覆盖/跳过/重命名"
    - "导入完成 Toast：「成功导入 N 条，跳过 M 条，覆盖 K 条」"
  artifacts:
    - path: "frontend/src/app/(main)/testcases/_components/import/FormatSelectStep.tsx"
      provides: "格式选择 + 模板下载步骤"
    - path: "frontend/src/app/(main)/testcases/_components/import/FieldMappingStep.tsx"
      provides: "字段映射步骤（必填校验）"
    - path: "frontend/src/app/(main)/testcases/_components/import/DuplicateStep.tsx"
      provides: "重复处理步骤"
    - path: "frontend/src/app/(main)/testcases/_components/import/ResultStep.tsx"
      provides: "导入结果展示步骤"
    - path: "frontend/src/app/(main)/testcases/_components/ImportDialog.tsx"
      provides: "重构后主文件（≤400行），组合子组件"
  key_links:
    - from: "ImportDialog.tsx 步骤机"
      to: "FormatSelectStep / FieldMappingStep / DuplicateStep / ResultStep"
      via: "currentStep state prop drilling"
      pattern: "FormatSelectStep|FieldMappingStep"
    - from: "DuplicateStep 覆盖确认"
      to: "/testcases/import POST with duplicate_action"
      via: "onConfirm callback → parent fetch"
      pattern: "duplicate_action"
---

<objective>
将 ImportDialog.tsx（1000 行 Fragile 组件）拆分为子组件，同时补全模板下载、必填字段校验、重复处理 UI。

Purpose: STATE.md 明确标注 ImportDialog 为 Fragile，必须先拆分再扩展，降低维护风险并实现 TC-07~11 需求。
Output: 1个精简主文件（≤400行）+ 4个子组件（各100-200行）。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md

<interfaces>
<!-- ImportDialog 架构（待确认） -->
当前 ImportDialog.tsx 1000 行，包含步骤：
1. 格式选择（FormatSelect）
2. 文件上传
3. 字段映射（ColumnMapping）
4. 预览（Preview）
5. 重复检测（Duplicate）
6. 结果（Result）

重构策略：提取 Step 组件，主文件只保留：
- Dialog 壳（open/onClose prop）
- currentStep state + stepper UI
- 跨步骤共享 state（selectedFormat, mappings, previewRows, duplicates, result）
- 各步骤组件渲染 + 步骤切换逻辑

子组件 Props 模式（data-in/callback-out）：
```typescript
interface FormatSelectStepProps {
  onSelect: (format: 'xlsx'|'csv'|'json'|'xmind') => void;
  selectedFormat: string | null;
}
interface FieldMappingStepProps {
  headers: string[];  // 文件列头
  mappings: Record<string, string>;  // target_field -> source_column
  onChange: (mappings: Record<string, string>) => void;
  onNext: () => void;
  requiredFields: string[];  // ['title']
}
interface DuplicateStepProps {
  duplicates: DuplicateInfo[];
  actions: Record<string, 'overwrite'|'skip'|'rename'>;
  onActionChange: (id: string, action: 'overwrite'|'skip'|'rename') => void;
  onConfirm: () => void;
}
interface ResultStepProps {
  result: { imported: number; skipped: number; overwritten: number };
  onClose: () => void;
}
```

已有 API：
```
/testcases/import/check-duplicates — POST {case_ids: string[]} → DuplicateInfo[]
/testcases/import — POST multipart/form-data with duplicate_action 参数
```
导入完成 API 响应：ImportResult {imported_count, skipped_count, overwritten_count}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 拆分 ImportDialog 为子组件架构（TC-07~11 前置重构）</name>
  <files>
    frontend/src/app/(main)/testcases/_components/ImportDialog.tsx,
    frontend/src/app/(main)/testcases/_components/import/FormatSelectStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/FieldMappingStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/PreviewStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/DuplicateStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/ResultStep.tsx
  </files>
  <action>
    先完整阅读 ImportDialog.tsx（重要，必须理解现有逻辑再动手）。

    创建 frontend/src/app/(main)/testcases/_components/import/ 目录。

    将对应 JSX 块提取到各子组件文件（不改变视觉效果和 API 调用逻辑，只移动代码）：
    - FormatSelectStep.tsx：格式选择 RadioGroup + 文件上传区域
    - FieldMappingStep.tsx：列头映射 Select 组件
    - PreviewStep.tsx：前5条预览表格
    - DuplicateStep.tsx：重复条目列表
    - ResultStep.tsx：完成结果展示

    主文件 ImportDialog.tsx 精简为：Dialog 壳 + step state + 共享 state + 子组件组合。
    目标：主文件行数 ≤ 400，每个子组件 ≤ 200 行。

    注意：
    - 不改变任何 API 调用逻辑，只移动 JSX 和相关 state
    - 提取后立即运行 tsc 验证，修复所有类型错误
    - 若子组件需要相同的类型定义，创建 import/types.ts 共享
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*(ImportDialog|import/)" | head -20</automated>
  </verify>
  <done>
    ImportDialog.tsx ≤ 400 行，import/ 目录有5个子组件文件，tsc 无错误，视觉效果与重构前一致
  </done>
</task>

<task type="auto">
  <name>Task 2: 补全导入功能：模板下载 + 必填校验 + 重复处理 Toast（TC-07, TC-08, TC-10, TC-11）</name>
  <files>
    frontend/src/app/(main)/testcases/_components/import/FormatSelectStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/FieldMappingStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/DuplicateStep.tsx,
    frontend/src/app/(main)/testcases/_components/import/ResultStep.tsx
  </files>
  <action>
    FormatSelectStep.tsx — 模板下载（TC-07）：
    在格式选择区域底部添加「下载对应模板」链接区域：
    ```typescript
    const templates = {
      xlsx: '/templates/testcase-template.xlsx',
      csv: '/templates/testcase-template.csv',
      json: '/templates/testcase-template.json',
      xmind: '/templates/testcase-template.xmind',
    }
    // 选中格式后显示对应下载链接
    ```
    注意：模板文件需在 public/templates/ 创建（创建最小有效文件，Excel/CSV 有表头行即可）。
    对于 xmind/xlsx 二进制模板，用 Python 脚本生成或创建空文件占位符均可。

    FieldMappingStep.tsx — 必填字段校验（TC-08）：
    - requiredFields 数组（至少 ['title']）
    - 计算 isValid = requiredFields.every(f => mappings[f] && mappings[f] !== '')
    - 「下一步」按钮 disabled={!isValid}，hover 显示 Tooltip "请映射必填字段：标题"
    - 必填字段行标注红色星号（text-sy-danger）

    DuplicateStep.tsx — 重复处理 UI（TC-10）：
    - 若已有 UI 但不完整，补充三选一：覆盖/跳过/重命名导入（Radio 或 Select）
    - 「覆盖」= overwrite existing，「跳过」= skip，「重命名」= 自动加 "_副本" 后缀
    - 「全部覆盖」/「全部跳过」批量操作按钮

    ResultStep.tsx — Toast 汇总（TC-11）：
    - 导入完成时调用 toast.success(`成功导入 ${result.imported_count} 条，跳过 ${result.skipped_count} 条，覆盖 ${result.overwritten_count} 条`)
    - Dialog 内也展示同样数字汇总（不只依赖 Toast）

    所有 UI 使用 sy-* 颜色 token，lucide-react 图标，sonner toast。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>
    tsc 错误数为 0，FormatSelectStep 含模板下载链接，FieldMappingStep 必填字段「下一步」按钮 disabled 逻辑存在，ResultStep 有 toast.success 调用
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit && wc -l src/app/\(main\)/testcases/_components/ImportDialog.tsx
```
tsc 无错误，ImportDialog.tsx 行数 ≤ 400。
</verification>

<success_criteria>
- ImportDialog.tsx ≤ 400 行，import/ 目录有子组件
- FormatSelectStep：格式选择 + 4种模板下载链接
- FieldMappingStep：title 未映射时「下一步」禁用
- DuplicateStep：每条重复可选择覆盖/跳过/重命名
- ResultStep：完成时 toast.success 含 N/M/K 数据
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-06-SUMMARY.md`
</output>
