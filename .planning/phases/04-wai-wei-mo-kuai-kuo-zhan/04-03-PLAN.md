---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "03"
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - backend/app/engine/rag/retriever.py
  - backend/app/modules/knowledge/router.py
  - backend/app/modules/knowledge/service.py
  - backend/app/modules/knowledge/schemas.py
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04

must_haves:
  truths:
    - "GET /knowledge/{id}/chunks 返回文档分块列表（含序号/token估算/内容前500字符），limit=50 分页"
    - "POST /knowledge/manual 创建手动知识条目，entry_type='manual'，自动向量化，chunk_count=1"
    - "上传新版本时旧版本 is_active=False，第4版本自动 soft-delete 最旧版本"
    - "KnowledgeDocument.category 使用四固定分类枚举：enterprise_standard/business_knowledge/historical_cases/tech_reference"
  artifacts:
    - path: "backend/app/engine/rag/retriever.py"
      provides: "scroll_by_doc_id 函数"
      contains: "scroll_by_doc_id"
    - path: "backend/app/modules/knowledge/router.py"
      provides: "chunks 端点 + manual 创建端点"
      contains: "/chunks"
    - path: "backend/app/modules/knowledge/service.py"
      provides: "create_manual_entry + upload_new_version 方法"
      contains: "create_manual_entry"
    - path: "backend/app/modules/knowledge/schemas.py"
      provides: "ManualEntryCreate schema + category enum"
      contains: "ManualEntryCreate"
  key_links:
    - from: "knowledge/router.py /{id}/chunks"
      to: "engine/rag/retriever.scroll_by_doc_id"
      via: "await scroll_by_doc_id(str(doc_id), limit, offset)"
      pattern: "scroll_by_doc_id"
    - from: "KnowledgeService.upload_new_version"
      to: "Qdrant delete_by_doc_id"
      via: "软删除最旧版本时清除向量"
      pattern: "delete_by_doc_id"
---

<objective>
补全知识库后端：分块预览接口、手动条目创建、版本管理限额、分类 enum 标准化。

Purpose: 为 Wave 2 知识库前端（KB-01~04）提供完整后端 API 支撑。
Output: retriever.py 新增 scroll_by_doc_id + knowledge 模块4个文件修改。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-01-SUMMARY.md

<interfaces>
<!-- 已有 KnowledgeService 方法 -->
```python
class KnowledgeService:
    async def upload_document(self, file: UploadFile) -> KnowledgeDocument: ...
    async def soft_delete(self, doc_id: UUID) -> bool: ...
    async def reindex_document(self, doc_id: UUID) -> KnowledgeDocument: ...
    def _build_chunks(self, doc: KnowledgeDocument) -> list: ...
```

KnowledgeDocument 已有字段（backend/app/modules/knowledge/models.py）：
- category: str（当前 default="business_rule"，需标准化为 enum）
- entry_type: str（manual/uploaded）
- is_active: bool
- version: int（递增）
- chunk_count: int
- vector_status: str（processing/completed/failed）

engine/rag 现有函数（retriever.py）：
- delete_by_doc_id(doc_id: str) — 删除 Qdrant points
- index_chunks(chunks: list, doc_id: str) — 向量化入库
- 需新增：scroll_by_doc_id(doc_id: str, limit=50, offset=0) -> list[dict]

Qdrant scroll API（用于 scroll_by_doc_id）：
```python
from qdrant_client.models import Filter, FieldCondition, MatchValue
results = await client.scroll(
    collection_name=COLLECTION,
    scroll_filter=Filter(must=[FieldCondition(key="doc_id", match=MatchValue(value=doc_id))]),
    limit=limit,
    offset=offset,
    with_payload=True,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scroll_by_doc_id + 分块预览端点（KB-02）</name>
  <files>backend/app/engine/rag/retriever.py, backend/app/modules/knowledge/router.py</files>
  <behavior>
    - scroll_by_doc_id(doc_id, limit=50, offset=0)：调用 Qdrant scroll，按 doc_id payload 过滤，返回 list[{"content": str, "chunk_index": int}]
    - GET /knowledge/{doc_id}/chunks?limit=50&offset=0 返回 {"items": [...], "total": N}
    - 每条 item 含 index（序号）、content（前500字符）、token_count（len(content.split()) 粗略估算）
    - 文档不存在时返回 {"items": [], "total": 0}（不报 404）
    - limit 最大 50，防止过大 payload
  </behavior>
  <action>
    检查 retriever.py 中 Qdrant client 获取方式（可能是 get_qdrant_client() 或全局单例）。
    在 retriever.py 末尾添加 scroll_by_doc_id 异步函数，复用已有 client 实例。
    chunk_index 从 payload 中读取（若 Qdrant payload 中有 chunk_index 字段），否则用 enumerate 生成序号。
    在 knowledge/router.py 添加 GET /{doc_id}/chunks 端点，调用 scroll_by_doc_id，构造响应。
    路由注意：/{doc_id}/chunks 要在 /{doc_id} 之前注册，避免路由冲突。
    limit 参数用 Query(default=50, le=50, ge=1) 限制。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_knowledge/test_chunks.py -v</automated>
  </verify>
  <done>测试全部 PASS，GET /knowledge/{id}/chunks 返回正确分块结构</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 手动条目创建 + 版本管理限额 + 分类 enum（KB-01, KB-03, KB-04）</name>
  <files>backend/app/modules/knowledge/schemas.py, backend/app/modules/knowledge/service.py, backend/app/modules/knowledge/router.py</files>
  <behavior>
    KB-01 分类 enum 标准化：
    - 四固定分类：enterprise_standard/business_knowledge/historical_cases/tech_reference
    - ManualEntryCreate.category 使用 Literal["enterprise_standard","business_knowledge","historical_cases","tech_reference"]

    KB-03 手动添加：
    - POST /knowledge/manual 接受 {title, category, content, tags[]}
    - 创建 KnowledgeDocument(entry_type="manual", is_active=True, vector_status="completed", chunk_count=1)
    - 立即向量化（调用 index_chunks）

    KB-04 版本管理：
    - POST /knowledge/{doc_id}/new-version 接受新文件，触发 upload_new_version
    - 同 title 的活跃版本 is_active=False，新版本 is_active=True
    - 活跃版本数超过 MAX_VERSIONS=3 时，soft-delete 最旧版本（deleted_at=now()）并清除 Qdrant 向量
  </behavior>
  <action>
    在 schemas.py 添加 ManualEntryCreate（title: str, category: Literal[...], content: str, tags: list[str] = []）。
    在 service.py 添加 create_manual_entry(data: ManualEntryCreate) 方法：创建 doc → flush → index_chunks → commit → refresh → return。
    在 service.py 添加 upload_new_version(doc_id, file) 方法：
      1. 找到该 doc 的 title，查所有同 title 且未删除的文档（按 version 升序）
      2. 全部设 is_active=False
      3. 若 len(versions) >= 3，取 versions[0] 设 deleted_at=datetime.now(UTC)，调用 delete_by_doc_id
      4. 调用 upload_document 逻辑创建新版本（version = max(versions)+1）
      5. 新版本 is_active=True
    在 router.py 添加：
      - POST /knowledge/manual → create_manual_entry
      - POST /knowledge/{doc_id}/new-version → upload_new_version（multipart/form-data）
    路由顺序：/manual 必须在 /{doc_id} 通配符路由之前注册。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_knowledge/test_manual_entry.py tests/unit/test_knowledge/test_version_limit.py -v</automated>
  </verify>
  <done>
    手动条目测试 PASS（entry_type="manual", vector_status="completed"），版本限额测试 PASS（第4版本时最旧版本 soft-deleted）
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_knowledge/ -v && uv run ruff check app/modules/knowledge/ app/engine/rag/retriever.py
```
全部 PASS，ruff 无 lint 错误。
</verification>

<success_criteria>
- GET /knowledge/{id}/chunks 返回分块列表（limit=50，含 token_count）
- POST /knowledge/manual 创建手动条目，entry_type="manual"，向量化完成
- POST /knowledge/{id}/new-version 触发版本限额逻辑，超3版 soft-delete 最旧
- 四固定分类 enum 定义在 ManualEntryCreate schema
- 所有知识库测试 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-03-SUMMARY.md`
</output>
