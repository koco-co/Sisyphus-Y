---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/templates/requirement-template.docx
  - frontend/public/templates/requirement-template.md
  - frontend/src/app/(main)/requirements/_components/UploadRequirementDialog.tsx
autonomous: true
requirements:
  - INP-01
  - INP-02
  - INP-03

must_haves:
  truths:
    - "需求上传对话框旁「下载模板」按钮可触发 docx/md 模板下载"
    - "UDA 解析完成后显示结构化条目确认 UI（条目列表 + 置信度），用户确认后才批量保存"
    - "置信度 < 0.7 的文档显示「识别置信度较低」Banner（黄色警告），但不阻断继续操作"
    - "无效文件格式（非 docx/pdf/md）上传时显示格式错误提示"
  artifacts:
    - path: "frontend/public/templates/requirement-template.docx"
      provides: "需求模板 Word 文件（最小有效 docx）"
    - path: "frontend/public/templates/requirement-template.md"
      provides: "需求模板 Markdown 文件"
    - path: "frontend/src/app/(main)/requirements/_components/UploadRequirementDialog.tsx"
      provides: "两步流程：上传解析 → 条目确认"
      contains: "parseStructure"
  key_links:
    - from: "UploadRequirementDialog.tsx"
      to: "/uda/parse-structure API"
      via: "fetch POST multipart/form-data"
      pattern: "parse-structure"
    - from: "UploadRequirementDialog.tsx 确认按钮"
      to: "/products/requirements/bulk-create"
      via: "fetch POST JSON"
      pattern: "bulk-create"
---

<objective>
实现需求录入优化：模板下载、UDA 解析结构确认 UI、置信度提示。

Purpose: 让测试工程师可下载标准模板填写需求，上传后通过两步确认流程（解析 → 确认）精确控制导入内容，非标准文档不阻断但给出置信度提示。
Output: 2个静态模板文件 + 升级后的 UploadRequirementDialog.tsx。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md

<interfaces>
<!-- UploadRequirementDialog 当前实现概要 -->
```typescript
// 当前：UploadRequirementDialog 已调用 /uda/parse-structure（line 84），但解析后直接保存
// 需要：在解析后显示确认 UI，用户确认才调用 bulk-create

// /uda/parse-structure 响应结构（已有 API）：
interface ParseStructureResponse {
  items: Array<{
    title: string;
    content: string;
    confidence: number;  // 0.0 ~ 1.0
    chapter?: string;
  }>;
  overall_confidence: number;
  parse_status: "success" | "partial" | "failed";
}

// /products/requirements/bulk-create（需确认是否已存在，若不存在则 executor 需调查）
interface BulkCreateRequest {
  items: Array<{ title: string; content: string; iteration_id?: string }>;
  iteration_id: string;
}
```

UI 设计规范（CLAUDE.md）：
- 使用 sy-* 颜色 token，禁止硬编码色值
- 置信度警告 Banner：bg-sy-warn/10 border border-sy-warn/35 text-sy-warn
- 确认步骤中低置信度条目：在列表项右侧显示 StatusBadge（variant="warn" text="低置信度"）
- 按钮 Loading：<Loader2 className="animate-spin" />
- 图标：lucide-react（无 emoji）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建需求模板文件（INP-01）</name>
  <files>frontend/public/templates/requirement-template.md</files>
  <action>
    在 frontend/public/templates/ 目录（不存在则创建）中创建两个模板文件：

    1. requirement-template.md — Markdown 格式需求模板，内容：
    ```markdown
    # 需求标题（必填）

    ## 功能描述
    （描述该需求的功能目标）

    ## 业务规则
    - 规则1
    - 规则2

    ## 输入条件
    （用户操作或系统触发条件）

    ## 预期结果
    （系统应该如何响应）

    ## 异常场景
    （边界条件和错误处理）
    ```

    2. requirement-template.docx — 最小有效 docx 文件。
    由于无法直接创建二进制 docx，创建一个 Python 脚本 scripts/create_templates.py 使用 python-docx 生成：
    ```python
    from docx import Document
    doc = Document()
    doc.add_heading("需求标题（必填）", 0)
    doc.add_heading("功能描述", 1)
    doc.add_paragraph("（描述该需求的功能目标）")
    doc.add_heading("业务规则", 1)
    doc.add_paragraph("- 规则1\n- 规则2")
    doc.add_heading("输入条件", 1)
    doc.add_paragraph("（用户操作或系统触发条件）")
    doc.add_heading("预期结果", 1)
    doc.add_paragraph("（系统应该如何响应）")
    doc.add_heading("异常场景", 1)
    doc.add_paragraph("（边界条件和错误处理）")
    doc.save("frontend/public/templates/requirement-template.docx")
    ```
    在项目根目录执行：`cd /Users/poco/Projects/Sisyphus-case-platform && python scripts/create_templates.py`（使用系统 Python，不是 uv）。
    若 python-docx 不在系统环境，改为用 uv run python scripts/create_templates.py（在 backend 目录）。
  </action>
  <verify>
    <automated>ls /Users/poco/Projects/Sisyphus-case-platform/frontend/public/templates/requirement-template.md /Users/poco/Projects/Sisyphus-case-platform/frontend/public/templates/requirement-template.docx</automated>
  </verify>
  <done>两个模板文件存在于 public/templates/，docx 文件大于 1KB（有效文件）</done>
</task>

<task type="auto">
  <name>Task 2: 升级 UploadRequirementDialog 为两步确认流程（INP-01, INP-02, INP-03）</name>
  <files>frontend/src/app/(main)/requirements/_components/UploadRequirementDialog.tsx</files>
  <action>
    先阅读完整的 UploadRequirementDialog.tsx（目前408行），理解现有上传逻辑后再修改。

    改造为两步流程：
    步骤 1（上传解析）：现有上传 UI + 新增「下载模板」链接按钮
    步骤 2（确认条目）：显示解析结果列表，用户可取消勾选，确认后批量保存

    具体修改：
    1. 添加 step: "upload" | "confirm" 状态（useState）。
    2. 在对话框顶部标题区旁添加「下载模板」下拉按钮（Popover 含两个选项：docx 和 md），使用 `<a href="/templates/requirement-template.docx" download>` 形式。
    3. 解析完成后（调用 /uda/parse-structure 后），将 step 切换为 "confirm"，显示：
       - 若 overall_confidence < 0.7：显示黄色 Banner「识别置信度较低（${(overall_confidence*100).toFixed(0)}%），请仔细核对条目内容」（bg-sy-warn/10 border border-sy-warn/35 text-sy-warn p-3 rounded-md）
       - 条目列表：Checkbox + 条目标题 + 内容预览（前60字符）+ 章节来源（若有）
       - 单条 confidence < 0.7 在右侧显示黄色「低置信度」徽章
    4. 「确认导入」按钮：调用 bulk-create API（仅提交 checked 的条目），先确认 /products/requirements/bulk-create 端点是否存在（GET /openapi.json 或直接检查 products/router.py）。若不存在，则逐条调用已有的 POST /products/{product_id}/iterations/{iter_id}/requirements 端点。
    5. 「返回修改」按钮：step 切回 "upload"。
    6. 对话框高度在 confirm 步骤需允许滚动（条目多时），添加 max-h-[60vh] overflow-y-auto 到条目列表容器。
    7. 确认导入时按钮显示 <Loader2 className="animate-spin" />，完成后 toast.success 并关闭对话框。

    注意：
    - 禁止硬编码颜色（不写 style={{ color: '#...' }}），全用 sy-* class
    - 图标用 lucide-react（Download, ChevronDown, CheckCircle, AlertTriangle）
    - 文件改造后行数可能增至500-600行（可接受），不超过800行上限
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS|UploadRequirementDialog" | head -20</automated>
  </verify>
  <done>
    TypeScript 无编译错误，UploadRequirementDialog 包含 step/confirm 状态逻辑，「下载模板」按钮存在，置信度 Banner 逻辑完整
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit && bunx biome check src/app/\(main\)/requirements/_components/UploadRequirementDialog.tsx
```
TypeScript 和 biome 无错误。
</verification>

<success_criteria>
- 模板文件可通过浏览器 /templates/requirement-template.docx 和 /templates/requirement-template.md 下载
- UploadRequirementDialog 两步流程完整（upload → confirm）
- 置信度 < 0.7 时显示黄色警告 Banner（不阻断）
- 用户可取消勾选不需要的条目后再确认导入
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-04-SUMMARY.md`
</output>
