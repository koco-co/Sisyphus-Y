---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "02"
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - backend/app/modules/dashboard/service.py
  - backend/app/modules/dashboard/router.py
  - backend/app/modules/testcases/models.py
  - backend/app/modules/diff/router.py
  - backend/app/modules/diff/service.py
  - backend/alembic/versions/add_change_impact_to_test_cases.py
autonomous: true
requirements:
  - DSH-02
  - DIF-04
  - DIF-05

must_haves:
  truths:
    - "GET /dashboard/trend 返回最近6迭代数据，含 iteration_name/testcase_count/p0_count/coverage_rate"
    - "Diff 完成后受影响用例的 change_impact 被自动设置（needs_rewrite/needs_review/not_affected）"
    - "POST /diff/{req_id}/push-to-workbench 将「需重写」用例批量推送，返回 pushed_count"
    - "alembic check 通过，迁移无噪声"
  artifacts:
    - path: "backend/app/modules/dashboard/service.py"
      provides: "get_trend_stats 方法"
      contains: "get_trend_stats"
    - path: "backend/app/modules/dashboard/router.py"
      provides: "/dashboard/trend GET 端点"
      contains: "/trend"
    - path: "backend/app/modules/testcases/models.py"
      provides: "TestCase.change_impact 字段"
      contains: "change_impact"
    - path: "backend/app/modules/diff/router.py"
      provides: "/diff/{req_id}/push-to-workbench POST 端点"
      contains: "push-to-workbench"
    - path: "backend/alembic/versions/add_change_impact_to_test_cases.py"
      provides: "change_impact 列迁移"
  key_links:
    - from: "dashboard/router.py"
      to: "DashboardService.get_trend_stats"
      via: "dependency injection session"
      pattern: "get_trend_stats"
    - from: "diff/router.py push-to-workbench"
      to: "TestCase 表 change_impact=needs_rewrite"
      via: "DiffService.push_to_workbench"
      pattern: "push_to_workbench"
---

<objective>
新增仪表盘趋势端点、TestCase 变更影响字段（DB 迁移）、Diff 推送工作台端点。

Purpose: 为 Wave 2 仪表盘折线图（DSH-02）和 Diff 推送功能（DIF-04、DIF-05）提供后端支撑。
Output: 3个后端模块修改 + 1个 Alembic 迁移文件。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-01-SUMMARY.md

<interfaces>
<!-- 关键已有类型 -->

DashboardService（backend/app/modules/dashboard/service.py）：
```python
class DashboardService:
    def __init__(self, session: AsyncSession) -> None: ...
    async def get_stats(self, iteration_id: UUID | None = None) -> DashboardStatsResponse: ...
    async def _get_iteration_options(self) -> list[IterationSnapshot]: ...
    async def _get_iteration_metrics(self, iteration_id: UUID) -> IterationMetrics: ...
    # 新增：
    async def get_trend_stats(self, product_id: UUID | None = None, limit: int = 6) -> list[dict]: ...
```

TestCase model（新增字段）：
```python
# backend/app/modules/testcases/models.py
class TestCase(BaseModel):
    # 现有字段：case_id, title, priority, source, steps, folder_id, ...
    # 新增：
    change_impact: Mapped[str | None] = mapped_column(String(20), nullable=True)
    # 值域: "needs_rewrite" | "needs_review" | "not_affected" | None
```

DiffService（backend/app/modules/diff/service.py）：
```python
class DiffService:
    async def _find_affected_test_cases(self, requirement_id: UUID) -> dict: ...
    async def regenerate_affected_cases(self, requirement_id: UUID, session_id: UUID) -> dict: ...
    # 新增：
    async def push_to_workbench(self, requirement_id: UUID) -> dict:
        # 找到 change_impact="needs_rewrite" 的用例，更新 status="pending_regen"，返回 pushed_count
```

IterationSnapshot（已有）：
```python
class IterationSnapshot:
    id: UUID
    name: str
    product_id: UUID
    version_major: int
    version_minor: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 仪表盘趋势端点（DSH-02 后端）</name>
  <files>backend/app/modules/dashboard/service.py, backend/app/modules/dashboard/router.py</files>
  <behavior>
    - get_trend_stats(product_id=None, limit=6)：查最近 limit 个迭代，时间正序，每条含 iteration_name/testcase_count/p0_count/coverage_rate
    - GET /dashboard/trend?product_id=&limit=6 返回 list[dict]，product_id 可选
    - 迭代无数据时 testcase_count=0, p0_count=0, coverage_rate=0.0（不返回空列表）
  </behavior>
  <action>
    在 DashboardService 末尾添加 get_trend_stats 方法，复用已有 _get_iteration_options 和 _get_iteration_metrics。
    按 _iteration_sort_key 排序后取最新 limit 个，再反转（时间正序）。
    P0 数量：子查询 count TestCase WHERE priority="P0" AND requirement.iteration_id=iter.id AND deleted_at IS NULL。
    在 router.py 添加端点 GET /dashboard/trend，接受 query param product_id（Optional[UUID]）和 limit（int, 1-24）。
    不要修改已有 get_stats 端点。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_dashboard/test_trend.py -v</automated>
  </verify>
  <done>测试全部 PASS，GET /dashboard/trend 返回正确结构</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestCase.change_impact 字段 + Diff 推送端点（DIF-04, DIF-05）</name>
  <files>
    backend/app/modules/testcases/models.py,
    backend/app/modules/diff/service.py,
    backend/app/modules/diff/router.py,
    backend/alembic/versions/add_change_impact_to_test_cases.py
  </files>
  <behavior>
    - TestCase 新增 change_impact: Mapped[str | None]，nullable，String(20)
    - mark_affected_test_cases（已在 engine/diff/impact_analyzer.py）调用后，TestCase.change_impact 被设置为 needs_rewrite/needs_review/not_affected
    - DiffService.push_to_workbench(req_id) 查找该需求下 change_impact="needs_rewrite" 的用例，更新 status="needs_regen"，返回 {"pushed_count": N}
    - POST /diff/{req_id}/push-to-workbench 端点调用 push_to_workbench，返回 {"pushed_count": N}
  </behavior>
  <action>
    前置：在 backend 目录执行 `uv run alembic check`，确认无冲突（输出 "No new upgrade operations detected" 即正常；若提示有未应用迁移先执行 `uv run alembic upgrade head`）。
    修改 TestCase 模型：在 folder_id 字段之后添加 change_impact 字段。
    生成迁移：`uv run alembic revision --autogenerate -m "add_change_impact_to_test_cases"`，检查生成文件只有 add_column 操作（无 DROP TABLE 噪声，若有则手动删除无关行）。
    执行迁移：`uv run alembic upgrade head`。
    在 DiffService 添加 push_to_workbench 方法：SELECT TestCase WHERE requirement_id in (reqs of that req_id) AND change_impact='needs_rewrite' AND deleted_at IS NULL，批量 UPDATE status='needs_regen'，commit，return {"pushed_count": len(cases)}。
    在 diff/router.py 添加 POST /{req_id}/push-to-workbench 端点。
    注意：diff router 前缀为 /diff，端点路径确认与已有端点不冲突。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run alembic check && uv run pytest tests/unit/test_diff/test_push_to_workbench.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    alembic check 无冲突，TestCase 表有 change_impact 列，Diff 推送工作台测试 PASS（test_push_to_workbench.py 全部通过），POST /diff/{req_id}/push-to-workbench 返回 {"pushed_count": N}
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_dashboard/test_trend.py tests/unit/test_diff/test_push_to_workbench.py -v && uv run alembic check
```
全部 PASS 且 alembic 无冲突。
</verification>

<success_criteria>
- GET /dashboard/trend 返回最近6迭代数据结构
- TestCase 表 change_impact 列已迁移，alembic check 通过
- Diff 完成后 change_impact 字段被自动填充（needs_rewrite/needs_review/not_affected）
- POST /diff/{req_id}/push-to-workbench 返回 pushed_count
- 所有相关测试 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-02-SUMMARY.md`
</output>
