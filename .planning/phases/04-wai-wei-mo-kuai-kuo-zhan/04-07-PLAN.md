---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "07"
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(main)/testcases/_components/ExportDialog.tsx
  - backend/app/modules/export/service.py
  - backend/app/modules/export/schemas.py
  - backend/app/modules/export/router.py
autonomous: true
requirements:
  - TC-12
  - TC-13
  - TC-14

must_haves:
  truths:
    - "ExportDialog 支持4种格式：xlsx/csv/xmind/md（含格式说明）"
    - "导出范围4选一：当前目录/按需求/按迭代/自由勾选用例 ID 列表"
    - "自定义字段复选框（XMind 格式固定全字段，其他格式可自选）"
    - "ExportJobCreate schema 的 scope 字段支持4种范围参数"
  artifacts:
    - path: "frontend/src/app/(main)/testcases/_components/ExportDialog.tsx"
      provides: "四范围四格式导出对话框"
      contains: "exportScope"
    - path: "backend/app/modules/export/schemas.py"
      provides: "ExportJobCreate schema with scope field"
      contains: "scope"
    - path: "backend/app/modules/export/service.py"
      provides: "按范围过滤用例导出逻辑"
  key_links:
    - from: "ExportDialog.tsx 确认导出"
      to: "POST /export/jobs API"
      via: "fetch POST {format, scope, scope_value, fields}"
      pattern: "export/jobs"
    - from: "ExportService"
      to: "TestCase 查询（按 folder_id/req_id/iteration_id/case_ids）"
      via: "scope 参数分支"
      pattern: "scope"
---

<objective>
完善用例导出功能：四种格式、四种范围、自定义字段选择。

Purpose: 实现 TC-12~14，让测试工程师可灵活导出用例（按目录/需求/迭代/勾选），满足对外交付需求。
Output: ExportDialog.tsx 升级 + 后端 scope 参数支持。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md

<interfaces>
<!-- ExportService 已有实现 -->
已有：ExportService.generate_xlsx(), generate_csv(), generate_json(), generate_xmind(), generate_md()

ExportJobCreate schema（待确认和扩展）：
```python
class ExportJobCreate(BaseModel):
    format: Literal["xlsx", "csv", "json", "xmind", "md"]
    scope: Literal["folder", "requirement", "iteration", "selected"]  # 新增或扩展
    scope_value: str | None = None  # folder_id/req_id/iteration_id
    case_ids: list[str] | None = None  # scope="selected" 时使用
    fields: list[str] | None = None  # 自定义字段列表，None=全字段
```

可选字段列表（前端复选框）：
title, module_path, precondition, priority, case_type, status, steps, tags

XMind 固定全字段（不可自定义）。

ExportService 当前查询（需扩展）：
```python
# 当前可能只支持按 iteration_id 过滤
# 需要扩展支持 folder_id、req_id、case_ids 过滤
# 新增 _get_cases_by_scope 方法：
async def _get_cases_by_scope(
    self, scope: str, scope_value: str | None, case_ids: list[str] | None
) -> list[TestCase]:
    if scope == "folder":
        # WHERE folder_id = scope_value AND deleted_at IS NULL
    elif scope == "requirement":
        # WHERE requirement_id = scope_value AND deleted_at IS NULL
    elif scope == "iteration":
        # 已有逻辑
    elif scope == "selected":
        # WHERE case_id IN (case_ids) AND deleted_at IS NULL
```

已有 ExportDialog.tsx：先读取当前行数和实现，了解已有格式选择 UI 后再扩展范围和字段选择。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端导出 scope 参数扩展（TC-13, TC-14）</name>
  <files>backend/app/modules/export/schemas.py, backend/app/modules/export/service.py, backend/app/modules/export/router.py</files>
  <action>
    先读取 export/schemas.py 和 export/service.py 理解现有实现。

    扩展 ExportJobCreate schema：
    - 添加 scope: Literal["folder", "requirement", "iteration", "selected"] 字段（默认 "iteration"，向后兼容）
    - 添加 scope_value: str | None（folder_id/req_id/iteration_id）
    - 添加 case_ids: list[str] | None（scope="selected" 时）
    - 添加 fields: list[str] | None（None=全字段，XMind 时忽略此参数）

    在 ExportService 添加 _get_cases_by_scope 私有方法（4分支）。
    各导出方法接收 case_ids 列表 + fields 参数，fields=None 时输出全字段。
    XMind 格式的 generate_xmind() 忽略 fields 参数，始终全字段。

    更新 router.py 的导出端点，使其读取新 scope 字段并路由到正确方法。

    类型检查：`cd backend && uv run pyright app/modules/export/`（允许有警告，无 error）。

    确认实际使用的 API 端点路径：读取 export/router.py，记录前缀和路径，在 action 完成后用 grep 确认 POST 端点存在（`grep -n "post\|POST" backend/app/modules/export/router.py`）。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_testcases/test_import_export.py -k "export" -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    ExportJobCreate schema 有 scope/scope_value/case_ids/fields 字段，ExportService 有 _get_cases_by_scope 方法支持4种 scope，pytest test_import_export.py -k export 全部通过，通过 `grep -n "def _get_cases_by_scope" backend/app/modules/export/service.py` 可确认方法存在
  </done>
</task>

<task type="auto">
  <name>Task 2: ExportDialog 四范围 + 自定义字段 UI（TC-12, TC-13, TC-14）</name>
  <files>frontend/src/app/(main)/testcases/_components/ExportDialog.tsx</files>
  <action>
    先读取 ExportDialog.tsx 了解现有 UI 结构。

    扩展 ExportDialog：

    格式选择区域（TC-12）：确认是否已有4种格式（xlsx/csv/xmind/md），若缺少 md 则添加。
    每种格式添加简短说明：xlsx=Excel表格、csv=纯文本、xmind=思维导图、md=Markdown。

    新增「导出范围」选择（TC-13）：
    ```typescript
    type ExportScope = 'folder' | 'requirement' | 'iteration' | 'selected'
    // 四个 RadioGroup 选项：
    // - 当前目录（需传入 currentFolderId prop）
    // - 按需求（展示需求下拉选择器，从 /products/requirements 获取）
    // - 按迭代（展示迭代下拉，从 /products/{id}/iterations 获取）
    // - 自由勾选（显示「请先在列表勾选用例」提示，需从父组件传入 selectedCaseIds）
    ```
    ExportDialog props 扩展：
    ```typescript
    interface ExportDialogProps {
      open: boolean;
      onClose: () => void;
      currentFolderId?: string;
      selectedCaseIds?: string[];  // CaseTable 多选
      iterationId?: string;        // 当前迭代上下文
    }
    ```

    新增「自定义字段」复选框（TC-14）：
    当格式不为 xmind 时，显示字段多选区域（8个可选字段复选框，默认全选）。
    XMind 格式时显示「XMind 格式导出全部字段」灰色提示。

    提交时构造 payload，并确认使用正确端点：
    ```typescript
    // 先读取 Task 1 中确认的实际 API 路径（从 export/router.py 的前缀+路径拼接）
    // 假设为 /export/jobs，若实际不同则以 router.py 中实际路径为准
    const payload = {
      format: selectedFormat,
      scope: exportScope,
      scope_value: getScopeValue(),
      case_ids: exportScope === 'selected' ? selectedCaseIds : null,
      fields: selectedFormat === 'xmind' ? null : selectedFields,
    }
    await fetch(`${API_BASE}/export/jobs`, { method: 'POST', body: JSON.stringify(payload) })
    // 若实际端点与 /export/jobs 不符，以 grep 结果为准，注释说明实际路径
    ```
    成功后 toast.success("导出任务已提交，文件准备好后自动下载")。

    UI 规范：sy-* 颜色 token，lucide-react 图标（Download, FileSpreadsheet, FileText, Map, FileDown）。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*ExportDialog" | head -10</automated>
  </verify>
  <done>
    ExportDialog.tsx TypeScript 无错误，含4种格式、4种导出范围选择、字段复选框（XMind 时隐藏），fetch 调用中可通过 grep 确认使用了具体的 API 端点 URL（如 `/export/jobs`）
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run ruff check app/modules/export/ && cd ../frontend && bunx tsc --noEmit 2>&1 | grep -c "error TS"
```
ruff 无错误，tsc 错误数为 0。
</verification>

<success_criteria>
- ExportJobCreate schema 有 scope/scope_value/case_ids/fields 4个新字段
- ExportService 有 _get_cases_by_scope 支持 folder/requirement/iteration/selected 4种范围过滤
- ExportDialog 展示4种格式选择、4种范围单选、字段多选（XMind 时禁用）
- 导出 API 调用使用了明确的端点 URL（代码中可 grep 到）
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-07-SUMMARY.md`
</output>
