---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_dashboard/test_trend.py
  - backend/tests/unit/test_knowledge/test_chunks.py
  - backend/tests/unit/test_knowledge/test_manual_entry.py
  - backend/tests/unit/test_knowledge/test_version_limit.py
  - backend/tests/unit/test_diff/test_push_to_workbench.py
  - backend/tests/unit/test_testcases/test_import_export.py
autonomous: true
requirements:
  - DSH-02
  - KB-02
  - KB-03
  - KB-04
  - DIF-04
  - DIF-05
  - TC-12
  - TC-13
  - TC-14

must_haves:
  truths:
    - "测试脚手架为 Wave 1 后端实现提供 RED 基线，执行后 6 个测试文件存在且全部 FAIL"
    - "测试用例覆盖 DSH-02 get_trend_stats 的6迭代数据结构"
    - "测试用例覆盖 KB-02 scroll_by_doc_id 的分块返回格式"
    - "测试用例覆盖 KB-03 create_manual_entry 的 entry_type 字段"
    - "测试用例覆盖 KB-04 第4版本上传时自动删除最旧版本"
    - "测试用例覆盖 DIF-04/DIF-05 push_to_workbench 推送逻辑和 mark_affected 标记"
    - "测试用例覆盖 TC-12/13/14 ExportService 四种 scope 过滤"
  artifacts:
    - path: "backend/tests/unit/test_dashboard/test_trend.py"
      provides: "DSH-02 trend stats 测试"
      exports: ["test_get_trend_stats_returns_six_iterations", "test_trend_stats_includes_p0_count"]
    - path: "backend/tests/unit/test_knowledge/test_chunks.py"
      provides: "KB-02 chunk 检索测试"
      exports: ["test_scroll_by_doc_id_returns_chunks", "test_chunks_limited_to_50"]
    - path: "backend/tests/unit/test_knowledge/test_manual_entry.py"
      provides: "KB-03 手动条目测试"
      exports: ["test_create_manual_entry_sets_entry_type", "test_create_manual_entry_vector_status"]
    - path: "backend/tests/unit/test_knowledge/test_version_limit.py"
      provides: "KB-04 版本限额测试"
      exports: ["test_fourth_version_deletes_oldest", "test_version_limit_is_three"]
    - path: "backend/tests/unit/test_diff/test_push_to_workbench.py"
      provides: "DIF-04/DIF-05 推送工作台测试"
      exports: ["test_push_to_workbench_returns_pushed_count", "test_mark_affected_sets_change_impact"]
    - path: "backend/tests/unit/test_testcases/test_import_export.py"
      provides: "TC-12/13/14 导入导出测试"
      exports: ["test_export_scope_folder", "test_export_scope_selected", "test_export_custom_fields"]
  key_links:
    - from: "test_trend.py"
      to: "DashboardService.get_trend_stats"
      via: "AsyncMock patch"
      pattern: "get_trend_stats"
    - from: "test_chunks.py"
      to: "scroll_by_doc_id"
      via: "AsyncMock patch of qdrant client"
      pattern: "scroll_by_doc_id"
    - from: "test_push_to_workbench.py"
      to: "DiffService.push_to_workbench"
      via: "AsyncMock patch of session"
      pattern: "push_to_workbench"
    - from: "test_import_export.py"
      to: "ExportService._get_cases_by_scope"
      via: "AsyncMock patch of session"
      pattern: "_get_cases_by_scope"
---

<objective>
创建 Wave 0 测试脚手架，为 Wave 1 后端新功能建立 RED 基线。

Purpose: 遵循 TDD 规范，确保后端新功能（DSH-02 趋势端点、KB-02 分块预览、KB-03 手动添加、KB-04 版本限额、DIF-04/05 推送工作台、TC-12~14 导入导出）实现前有可运行的失败测试。
Output: 6个测试文件，pytest 执行均处于 FAIL 状态（ImportError 或 NotImplementedError 均可）。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md

<interfaces>
<!-- 测试文件的目标函数签名（Wave 1 实现后需满足） -->

DashboardService 新增方法（待实现）：
```python
async def get_trend_stats(self, product_id: UUID | None = None, limit: int = 6) -> list[dict]:
    # 返回: [{"iteration_name": str, "testcase_count": int, "p0_count": int, "coverage_rate": float}]
```

KnowledgeService 新增方法（待实现）：
```python
async def create_manual_entry(self, data: ManualEntryCreate) -> KnowledgeDocument:
    # entry_type="manual", vector_status="completed", chunk_count=1
async def upload_new_version(self, doc_id: UUID, file: UploadFile) -> KnowledgeDocument:
    # 最多3版，超限自动 soft-delete 最旧版本

```

engine/rag/retriever 新增函数（待实现）：
```python
async def scroll_by_doc_id(doc_id: str, limit: int = 50, offset: int = 0) -> list[dict]:
    # 返回: [{"content": str, "chunk_index": int}]
```

DiffService 新增方法（待实现）：
```python
async def push_to_workbench(self, requirement_id: UUID) -> dict:
    # 找到 change_impact="needs_rewrite" 的用例，批量更新 status="needs_regen"
    # 返回: {"pushed_count": int}
async def mark_affected_test_cases(self, requirement_id: UUID, impact_map: dict) -> None:
    # 根据 impact_map 更新 TestCase.change_impact 字段
```

ExportService 新增方法（待实现）：
```python
async def _get_cases_by_scope(self, scope: str, scope_value: str | None, case_ids: list[str] | None) -> list[TestCase]:
    # scope in ["folder", "requirement", "iteration", "selected"]
```

现有测试目录参考：
- backend/tests/unit/test_dashboard/ — 已存在目录，可能有 conftest.py
- backend/tests/unit/test_knowledge/ — 已存在目录
- backend/tests/unit/test_diff/ — 已存在目录（检查是否有 __init__.py 和 conftest.py）
- backend/tests/unit/test_testcases/ — 已存在目录
</interfaces>
</context>

<feature>
  <name>Wave 0 测试脚手架（6 个测试文件）</name>
  <files>
    backend/tests/unit/test_dashboard/test_trend.py,
    backend/tests/unit/test_knowledge/test_chunks.py,
    backend/tests/unit/test_knowledge/test_manual_entry.py,
    backend/tests/unit/test_knowledge/test_version_limit.py,
    backend/tests/unit/test_diff/test_push_to_workbench.py,
    backend/tests/unit/test_testcases/test_import_export.py
  </files>
  <behavior>
    test_dashboard/test_trend.py:
    - test_get_trend_stats_returns_six_iterations: mock 6个迭代快照，断言返回列表长度=6
    - test_trend_stats_correct_structure: 断言每条有 iteration_name/testcase_count/p0_count/coverage_rate
    - test_trend_stats_time_ascending: 断言列表按时间正序排列

    test_knowledge/test_chunks.py:
    - test_scroll_by_doc_id_returns_chunks: mock Qdrant scroll，断言返回 list[dict] 含 content/chunk_index
    - test_chunks_limited_to_50: 断言默认 limit=50
    - test_chunks_offset: 断言 offset 参数正确传递

    test_knowledge/test_manual_entry.py:
    - test_create_manual_entry_sets_entry_type: 断言 KnowledgeDocument.entry_type == "manual"
    - test_create_manual_entry_vector_status_completed: 断言 vector_status == "completed" 且 chunk_count == 1
    - test_create_manual_entry_category_validation: 断言非法 category 抛出 ValueError

    test_knowledge/test_version_limit.py:
    - test_fourth_version_deletes_oldest: 上传第4个版本时断言最旧版本被软删除（deleted_at != None）
    - test_version_limit_is_three: 断言 MAX_VERSIONS == 3
    - test_old_versions_set_inactive: 上传新版本后，旧版本 is_active == False

    test_diff/test_push_to_workbench.py:
    - test_push_to_workbench_returns_pushed_count: mock 3条 needs_rewrite 用例，断言返回 {"pushed_count": 3}
    - test_push_to_workbench_only_needs_rewrite: 断言只更新 change_impact="needs_rewrite" 的用例（not_affected 不变）
    - test_mark_affected_sets_change_impact: 断言 mark_affected 后 TestCase.change_impact 被正确设置

    test_testcases/test_import_export.py:
    - test_export_scope_folder: mock session，断言 _get_cases_by_scope("folder", folder_id, None) 查询含 folder_id 过滤
    - test_export_scope_selected: 断言 _get_cases_by_scope("selected", None, case_ids) 查询含 IN 过滤
    - test_export_custom_fields: 断言 fields=["title","priority"] 时生成结果只含这两列（不含 steps 等）
  </behavior>
  <implementation>
    1. 检查各测试目录是否存在 conftest.py 和 __init__.py，若有，读取复用已有 fixture。
    2. 对于 test_diff/ 和 test_testcases/ 目录，若不存在 __init__.py 则创建空文件。
    3. 创建6个测试文件，每个文件顶部 import 目标函数/类（此时会 ImportError 或 AttributeError，即 RED 状态）。
    4. 使用 pytest.mark.asyncio + unittest.mock.AsyncMock 模拟 DB Session 和 Qdrant client。
    5. RED 验证：依次运行各测试文件，应全部 FAIL/ERROR。
    6. 提交（不实现业务代码）。
  </implementation>
</feature>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_dashboard/test_trend.py tests/unit/test_knowledge/test_chunks.py tests/unit/test_knowledge/test_manual_entry.py tests/unit/test_knowledge/test_version_limit.py tests/unit/test_diff/test_push_to_workbench.py tests/unit/test_testcases/test_import_export.py -v 2>&1 | tail -30
```
预期：6个文件全部被发现，测试项全部 FAILED/ERROR（RED 状态）。
</verification>

<success_criteria>
- 6个测试文件存在且格式正确（可被 pytest 发现）
- `uv run pytest` 发现至少18个测试项（每文件3个）
- 所有测试处于 RED 状态（FAIL 或 ERROR，不得 PASS）
- 没有语法错误（文件可被 Python 导入）
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-01-SUMMARY.md`
</output>
