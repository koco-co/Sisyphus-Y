---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "10"
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - frontend/src/app/(main)/knowledge/_components/ChunkPreviewDrawer.tsx
  - frontend/src/app/(main)/knowledge/_components/ManualEntryDialog.tsx
  - frontend/src/app/(main)/knowledge/_components/DocTable.tsx
  - frontend/src/app/(main)/knowledge/page.tsx
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04

must_haves:
  truths:
    - "知识库列表4个固定分类 Tab 可筛选（企业测试规范/业务领域知识/历史用例/技术参考）"
    - "点击文档行展开 ChunkPreviewDrawer，显示分块列表（序号/token估算/内容前500字符）"
    - "「手动添加条目」按钮弹 ManualEntryDialog，填写标题/分类/内容后保存，条目显示「手动」Badge"
    - "上传新版本时弹确认对话框，旧版本被停用，版本号显示在列表中，超3版提示将自动清理"
  artifacts:
    - path: "frontend/src/app/(main)/knowledge/_components/ChunkPreviewDrawer.tsx"
      provides: "分块预览抽屉（序号、token、内容）"
      contains: "ChunkPreviewDrawer"
    - path: "frontend/src/app/(main)/knowledge/_components/ManualEntryDialog.tsx"
      provides: "手动添加知识条目对话框"
      contains: "ManualEntryDialog"
    - path: "frontend/src/app/(main)/knowledge/_components/DocTable.tsx"
      provides: "文档列表（含分类筛选、版本、手动 Badge、新版本上传）"
    - path: "frontend/src/app/(main)/knowledge/page.tsx"
      provides: "知识库页面（集成4分类 Tab + 手动添加按钮）"
  key_links:
    - from: "DocTable.tsx 行点击"
      to: "ChunkPreviewDrawer（open=true, docId）"
      via: "selectedDocId state"
      pattern: "ChunkPreviewDrawer"
    - from: "ChunkPreviewDrawer"
      to: "GET /knowledge/{docId}/chunks"
      via: "fetch on open"
      pattern: "/chunks"
    - from: "ManualEntryDialog 确认"
      to: "POST /knowledge/manual"
      via: "fetch POST {title, category, content, tags}"
      pattern: "/knowledge/manual"
---

<objective>
实现知识库前端：四分类 Tab 筛选、分块预览抽屉、手动条目对话框、版本管理 UI。

Purpose: 实现 KB-01~04，让测试工程师可精细管理知识库（按分类浏览、预览向量化分块、手动添加条目、管理版本）。
Output: 2个新组件（ChunkPreviewDrawer、ManualEntryDialog）+ DocTable 和 page.tsx 升级。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-03-SUMMARY.md

<interfaces>
<!-- 知识库后端 API（04-03 实现后可用） -->
```typescript
// GET /knowledge?category=enterprise_standard — 按分类过滤
// GET /knowledge/{id}/chunks?limit=50&offset=0 — 分块预览
// POST /knowledge/manual — 手动添加条目（{title, category, content, tags}）
// POST /knowledge/{id}/new-version — 上传新版本（multipart/form-data）

// 分块数据结构（04-03 实现）：
interface ChunkItem {
  index: number;
  content: string;  // 前500字符
  token_count: number;
}
interface ChunksResponse {
  items: ChunkItem[];
  total: number;
}

// 知识库分类（04-03 定义）：
type KnowledgeCategory =
  | 'enterprise_standard'  // 企业测试规范
  | 'business_knowledge'   // 业务领域知识
  | 'historical_cases'     // 历史用例
  | 'tech_reference';      // 技术参考
```

<!-- 已有知识库组件 -->
DocTable.tsx — 已存在（读取内容后了解现有 UI）
UploadDialog.tsx — 已存在（文件上传）
page.tsx — 已存在

UI 规范：
- 分类 Tab：使用 shadcn/ui Tabs 组件
- 抽屉：使用 shadcn/ui Sheet（side="right"，宽 640px）
- 手动Badge：StatusBadge component（bg-sy-bg-3 border border-sy-border-2 text-sy-text-3）或类似
- version 列：显示 v{version} 标记
- 图标：FileText, Eye, Plus, Upload（lucide-react）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChunkPreviewDrawer 组件（KB-02）</name>
  <files>frontend/src/app/(main)/knowledge/_components/ChunkPreviewDrawer.tsx</files>
  <action>
    创建 ChunkPreviewDrawer.tsx：

    ```typescript
    interface ChunkPreviewDrawerProps {
      open: boolean;
      docId: string | null;
      docTitle: string;
      onClose: () => void;
    }
    ```

    UI 结构（Sheet side="right"，宽 w-[640px]）：
    - Header：文档标题 + 总分块数（「共 N 个分块」）
    - 分块列表（ScrollArea）：每条 chunk 显示：
      - 序号 Badge（#01, #02...，font-mono bg-sy-bg-3）
      - token 估算（text-sy-text-3 text-xs，「约 N 词」）
      - 内容文本（text-sy-text text-sm，前500字符，超出省略）
      - 内容若超120字符显示「展开」/「收起」切换
    - 分页：若 total > 50，显示「加载更多」按钮（offset 递增50）
    - 加载状态：3行 Skeleton 占位
    - 空状态：「该文档暂无分块，可能正在向量化中」（text-sy-text-3）

    fetch 逻辑：
    - open=true 且 docId 变化时，重新 fetch GET /knowledge/{docId}/chunks?limit=50
    - 加载更多时 offset += 50，追加到 items 列表（不覆盖）

    颜色规范（sy-* token）：
    - 序号 Badge：bg-sy-bg-3 text-sy-text-2 border border-sy-border
    - 内容背景：bg-sy-bg-2 rounded p-3
    - 分隔线：border-sy-border
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*ChunkPreviewDrawer" | head -5</automated>
  </verify>
  <done>ChunkPreviewDrawer.tsx TypeScript 无错误，Sheet 含分块列表、分页、loading 骨架屏</done>
</task>

<task type="auto">
  <name>Task 2: ManualEntryDialog + DocTable 升级 + page.tsx 集成（KB-01, KB-03, KB-04）</name>
  <files>
    frontend/src/app/(main)/knowledge/_components/ManualEntryDialog.tsx,
    frontend/src/app/(main)/knowledge/_components/DocTable.tsx,
    frontend/src/app/(main)/knowledge/page.tsx
  </files>
  <action>
    先阅读 DocTable.tsx 和 page.tsx 了解现有结构。

    创建 ManualEntryDialog.tsx（KB-03）：
    ```typescript
    interface ManualEntryDialogProps {
      open: boolean;
      onClose: () => void;
      onSuccess: () => void;  // 刷新列表
    }
    ```
    表单字段（使用 shadcn/ui Form 或受控 state）：
    - 标题（必填，Input）
    - 分类（必填，Select：企业测试规范/业务领域知识/历史用例/技术参考）
    - 内容（必填，Textarea 最小高度 120px）
    - 标签（可选，Tag 输入，用逗号分隔解析为数组）
    提交：POST /knowledge/manual，成功后 toast.success("知识条目已添加") + onSuccess() + onClose()。
    Loading：Loader2 animate-spin。

    升级 DocTable.tsx（KB-01, KB-04）：
    - 列表新增「版本」列（v{version}，text-sy-text-3 font-mono text-xs）
    - entry_type="manual" 的条目右侧显示「手动」Badge（小徽章，bg-sy-bg-3 text-sy-text-2）
    - 操作列新增「查看分块」按钮（Eye icon）→ 触发 onPreviewChunks(docId, docTitle) callback
    - 操作列新增「上传新版本」按钮（Upload icon）→ 弹确认对话框（「此操作将停用当前版本，是否继续？」）→ 调用 POST /knowledge/{id}/new-version（复用 UploadDialog 或 file input）
    - 版本超过3个时，在「上传新版本」Tooltip 中显示「将自动清理最旧版本」提示

    升级 knowledge/page.tsx（KB-01）：
    - 在文档列表上方添加分类 Tab（shadcn/ui Tabs）：全部/企业测试规范/业务领域知识/历史用例/技术参考
    - 选中 Tab 时，向 /knowledge?category={cat} 发起 fetch（空分类 = 全部）
    - 右上角添加「手动添加条目」Button（Plus icon）→ setManualOpen(true)
    - 渲染 ManualEntryDialog（onSuccess 刷新文档列表）
    - 渲染 ChunkPreviewDrawer（selectedDocId state 驱动）
    - DocTable 的 onPreviewChunks callback 设置 selectedDocId

    sy-* 颜色规范，lucide-react 图标，无 emoji。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*(ManualEntry|DocTable|knowledge)" | head -10</automated>
  </verify>
  <done>
    ManualEntryDialog TypeScript 无错误，DocTable 含「查看分块」和「上传新版本」操作，page.tsx 有分类 Tab 和「手动添加条目」按钮，ChunkPreviewDrawer 集成完整
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit && bunx biome check "src/app/(main)/knowledge/" --reporter=summary 2>&1 | tail -5
```
TypeScript 和 biome 无错误。
</verification>

<success_criteria>
- ChunkPreviewDrawer.tsx：Sheet 组件，分块列表含序号/token/内容，支持分页加载
- ManualEntryDialog.tsx：4字段表单，调用 POST /knowledge/manual
- DocTable.tsx：含版本列、「手动」Badge、「查看分块」和「上传新版本」操作
- page.tsx：5个分类 Tab + 「手动添加条目」按钮 + 两个 Dialog 集成
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-10-SUMMARY.md`
</output>
