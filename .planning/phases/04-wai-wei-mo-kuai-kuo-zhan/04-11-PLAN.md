---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "11"
type: execute
wave: 3
depends_on: ["04-05", "04-06", "04-07", "04-08", "04-09", "04-10"]
files_modified: []
autonomous: false
requirements:
  - INP-01
  - INP-02
  - INP-03
  - TC-01
  - TC-02
  - TC-03
  - TC-04
  - TC-05
  - TC-06
  - TC-07
  - TC-08
  - TC-09
  - TC-10
  - TC-11
  - TC-12
  - TC-13
  - TC-14
  - DSH-01
  - DSH-02
  - DSH-03
  - DSH-04
  - DSH-05
  - DSH-06
  - DIF-01
  - DIF-02
  - DIF-03
  - DIF-04
  - DIF-05
  - KB-01
  - KB-02
  - KB-03
  - KB-04

must_haves:
  truths:
    - "上传需求旁「下载模板」按钮可用，解析后两步确认流程正常，置信度提示不阻断"
    - "用例库目录双击重命名（重名校验），右键「移动到…」弹对话框，「未分类」目录受保护"
    - "导入流程走完5步（格式→映射→预览→重复→完成），模板可下载，必填字段校验生效"
    - "导出支持4格式4范围，自定义字段 XMind 时固定全字段"
    - "仪表盘折线图和环形图正常渲染，迭代选择器切换后全部图表同步更新"
    - "Diff 并排视图可用，发布新版本入口存在，受影响用例显示正确 Badge，一键推送有效"
    - "知识库分类 Tab 切换，分块预览抽屉展开正常，手动条目保存成功"
  artifacts:
    - path: "frontend（build 成功）"
      provides: "前端编译无错误"
    - path: "backend（pytest green）"
      provides: "后端测试全部通过"
---

<objective>
Phase 4 端到端验收 checkpoint：人工验证6大模块核心流程均可正常使用。

Purpose: Phase 4 包含6个模块34项需求，自动化测试覆盖后端逻辑，但核心 UI 交互（拖拽、图表渲染、抽屉展开、多步骤导入）必须人工确认。
Output: Phase 4 验收通过，可进入 Phase 5 体验收尾。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全套自动化验证</name>
  <files></files>
  <action>
    依次运行所有自动化验证命令：

    1. 后端测试全套：
    ```bash
    cd /Users/poco/Projects/Sisyphus-case-platform/backend
    uv run pytest tests/unit/test_dashboard/ tests/unit/test_diff/ tests/unit/test_knowledge/ tests/unit/test_testcases/ -v --tb=short
    ```

    2. 后端 lint + 类型检查：
    ```bash
    uv run ruff check app/modules/dashboard/ app/modules/diff/ app/modules/knowledge/ app/modules/export/ app/modules/products/
    ```

    3. 前端类型检查 + 构建：
    ```bash
    cd /Users/poco/Projects/Sisyphus-case-platform/frontend
    bunx tsc --noEmit
    bun run build
    ```

    4. alembic 迁移状态确认：
    ```bash
    cd /Users/poco/Projects/Sisyphus-case-platform/backend
    uv run alembic check
    ```

    如有失败，记录失败项并修复后重新运行。目标：全部通过。
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_dashboard/ tests/unit/test_diff/ tests/unit/test_knowledge/ -q --tb=line 2>&1 | tail -5</automated>
  </verify>
  <done>后端测试全部 PASS，前端 build 成功，alembic check 通过</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 全部6模块已实现：
    1. 需求录入（INP）：模板下载 + UDA 两步确认 + 置信度提示
    2. 用例库目录管理（TC-01~06）：重命名校验 + 右键移动 + 未分类保护
    3. 用例库导入导出（TC-07~14）：ImportDialog 拆分 + 必填校验 + 重复处理 + 四范围导出 + 自定义字段
    4. 仪表盘（DSH）：折线图 + 环形图 + 迭代选择器联动 + 骨架屏
    5. 需求 Diff（DIF）：发布新版本 + 并排视图 + change_impact Badge + 推送工作台
    6. 知识库（KB）：四分类 Tab + 分块预览抽屉 + 手动添加 + 版本管理
  </what-built>
  <how-to-verify>
    请启动开发环境（./init.sh 或 cd backend && uv run uvicorn ... + cd frontend && bun dev），然后逐项验证：

    **需求录入（2分钟）：**
    1. 访问 /requirements，点击「上传需求」
    2. 确认对话框右上角有「下载模板」按钮（可下载 docx 和 md）
    3. 上传一个需求文档，确认解析后显示条目列表（步骤2确认），有置信度提示

    **用例库目录（3分钟）：**
    1. 访问 /testcases，在左侧目录树双击一个目录，修改名称（回车确认）
    2. 尝试将目录重命名为同级已存在的目录名，确认有错误提示
    3. 右键点击一个非未分类目录，确认菜单中有「移动到…」选项
    4. 点击「移动到…」，确认弹出目标目录选择对话框
    5. 右键点击「未分类」目录，确认没有「重命名」「删除」「移动到…」选项

    **仪表盘（2分钟）：**
    1. 访问首页 /，确认4卡片正常渲染数据（非零值或0）
    2. 确认中部有折线图（TrendChart），显示迭代趋势
    3. 确认有环形图（SourcePieChart），显示用例来源占比
    4. 切换右上角迭代选择器，确认卡片数据和图表同步更新

    **Diff（2分钟）：**
    1. 访问 /requirements，打开一个需求编辑器，确认右上角工具栏有「发布新版本」按钮
    2. 访问 /diff，选择一个已有 Diff 的需求
    3. 确认文本对比 Tab 显示左右并排视图（而非单列原始文本）
    4. 确认受影响用例列表每条有 change_impact Badge（颜色：红/黄/绿）
    5. 若有 needs_rewrite 用例，确认「一键推送到工作台」按钮可点击

    **知识库（2分钟）：**
    1. 访问 /knowledge，确认顶部有5个 Tab（全部/企业测试规范/业务领域知识/历史用例/技术参考）
    2. 切换 Tab，确认文档列表按分类筛选
    3. 点击一条文档的「查看分块」按钮，确认右侧抽屉展开，显示分块列表
    4. 点击「手动添加条目」，填写表单，确认保存后列表出现新条目且有「手动」Badge

    如有问题，请描述具体失败的步骤和错误信息。
  </how-to-verify>
  <resume-signal>输入 "approved" 确认 Phase 4 验收通过，或描述发现的具体问题</resume-signal>
</task>

</tasks>

<verification>
所有6模块核心流程人工验收通过：
- 需求录入两步确认流程
- 用例库目录重命名/移动/未分类保护
- 仪表盘折线图+环形图渲染
- Diff 并排视图+推送工作台
- 知识库分类Tab+分块预览
</verification>

<success_criteria>
- 后端 pytest 全部 PASS（test_dashboard + test_diff + test_knowledge + test_testcases）
- 前端 tsc --noEmit 无错误，bun run build 成功
- alembic check 通过
- 人工验收：6大模块核心交互流程均可正常操作
- ROADMAP.md Phase 4 状态更新为 Complete
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-11-SUMMARY.md`
2. Update `.planning/ROADMAP.md` Phase 4 status to Complete with completion date
3. Update `.planning/STATE.md` current phase to Phase 5
</output>
