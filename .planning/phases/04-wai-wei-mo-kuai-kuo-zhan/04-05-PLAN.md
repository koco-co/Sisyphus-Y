---
phase: 04-wai-wei-mo-kuai-kuo-zhan
plan: "05"
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(main)/testcases/_components/FolderTree.tsx
  - frontend/src/app/(main)/testcases/_components/MoveFolderDialog.tsx
  - frontend/src/app/(main)/testcases/_components/MoveCaseDialog.tsx
autonomous: true
requirements:
  - TC-01
  - TC-02
  - TC-03
  - TC-04
  - TC-05
  - TC-06

must_haves:
  truths:
    - "目录树支持双击重命名（回车确认/Esc取消），名称不能为空或与同级重名，超20字截断"
    - "目录删除：有用例时弹二次确认，无用例直接删；软删除链路完整"
    - "同级目录拖拽排序（dnd-kit）持久化 sort_order"
    - "右键菜单包含「移动到…」选项，弹出 MoveFolderDialog/MoveCaseDialog"
    - "「未分类」目录（is_system=true）无重命名/删除/移动选项"
    - "跨层级移动目录/用例通过独立 Dialog 选择目标目录"
  artifacts:
    - path: "frontend/src/app/(main)/testcases/_components/FolderTree.tsx"
      provides: "完整目录树，含右键菜单「移动到…」"
      contains: "MoveFolderDialog"
    - path: "frontend/src/app/(main)/testcases/_components/MoveFolderDialog.tsx"
      provides: "目录移动对话框"
    - path: "frontend/src/app/(main)/testcases/_components/MoveCaseDialog.tsx"
      provides: "用例移动对话框"
  key_links:
    - from: "FolderTree.tsx 右键「移动到…」"
      to: "MoveFolderDialog / MoveCaseDialog"
      via: "state: movingFolderId / movingCaseIds"
      pattern: "MoveFolderDialog|MoveCaseDialog"
    - from: "MoveCaseDialog 确认"
      to: "/testcases/folders/move-cases API"
      via: "fetch POST {case_ids, target_folder_id}"
      pattern: "move-cases"
---

<objective>
补全用例库目录树：右键「移动到…」Dialog、重命名重名校验、拖拽持久化。

Purpose: 实现 TC-01~06 所有目录管理交互，FolderTree 功能完整可用。
Output: FolderTree.tsx 增强 + 2个新增 Dialog 组件。
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-RESEARCH.md

<interfaces>
<!-- FolderTree 已有功能（勿重复实现） -->
已实现：
- 双击重命名（回车确认/Esc 取消）— 需补充重名验证
- 拖拽排序（dnd-kit）— 需确认 sort_order 是否已持久化
- 删除弹窗（deleteConfirm）— 需确认级联软删除链路
- is_system=true 禁用操作 — 已实现

待实现：
- 右键菜单「移动到…」选项
- 重名校验（同级目录不允许同名）
- 用例拖拽到不同目录（TC-05 — 当前 FolderTree 无用例拖拽）

已有 API 端点：
```
POST /testcases/folders/move-cases — 批量移动用例
  body: {case_ids: string[], target_folder_id: string}
PATCH /testcases/folders/{folder_id} — 更新 folder（含 parent_id, sort_order）
```

lucide-react 图标（禁止 emoji）：
- FolderOpen, Folder, MoreHorizontal, Edit2, Trash2, MoveRight, Plus

dnd-kit（已安装）：
```typescript
import { DndContext, closestCenter } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy, useSortable } from '@dnd-kit/sortable'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FolderTree 重名校验 + 右键菜单「移动到…」（TC-02, TC-04）</name>
  <files>frontend/src/app/(main)/testcases/_components/FolderTree.tsx</files>
  <action>
    先完整阅读 FolderTree.tsx，理解已有 renaming/context menu 状态逻辑。

    修改重名校验：
    - 在重命名 onConfirm 回调中，检查同级 siblings（同 parent_id）中是否已存在同名目录（大小写不敏感）
    - 若重名，toast.error("目录名称已存在") 并保持编辑状态（不关闭）
    - 超20字：trim 后截断为20字符（不报错，静默截断）

    新增「移动到…」右键菜单项：
    - 右键（或 ContextMenu）菜单中，在「删除」之前添加「移动到…」MenuItem
    - 图标：MoveRight from lucide-react
    - is_system=true 的目录不显示「移动到…」
    - 点击时：setMovingFolderId(folder.id)（新增 state），渲染 MoveFolderDialog

    重构注意：
    - FolderTree.tsx 当前行数未知，先读文件，若已超 700 行则将 ContextMenu 部分提取为 FolderContextMenu 子组件
    - 禁止在 FolderTree.tsx 内嵌 Dialog 的完整 JSX，使用 <MoveFolderDialog> 组件
    - 不改变已有的拖拽/删除逻辑
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*FolderTree" | head -10</automated>
  </verify>
  <done>FolderTree.tsx TypeScript 无错误，包含 movingFolderId state，右键菜单有「移动到…」选项</done>
</task>

<task type="auto">
  <name>Task 2: MoveFolderDialog + MoveCaseDialog 组件（TC-04, TC-05）</name>
  <files>
    frontend/src/app/(main)/testcases/_components/MoveFolderDialog.tsx,
    frontend/src/app/(main)/testcases/_components/MoveCaseDialog.tsx
  </files>
  <action>
    创建 MoveFolderDialog.tsx：
    ```typescript
    interface MoveFolderDialogProps {
      open: boolean;
      folderId: string;
      currentParentId: string | null;
      folders: TestCaseFolder[];  // 全部目录列表，用于渲染目标目录树
      onClose: () => void;
      onSuccess: () => void;  // 触发父组件刷新
    }
    ```
    UI：Dialog 内展示可选目录树（排除当前目录及其子目录），用户点选目标目录后「确认移动」。
    API：PATCH /testcases/folders/{folderId}，body: {parent_id: targetFolderId}。
    成功后 toast.success("目录已移动") + onSuccess() + onClose()。
    超3层级时不可选（显示置灰 + Tooltip "超过3层限制"）。

    创建 MoveCaseDialog.tsx：
    ```typescript
    interface MoveCaseDialogProps {
      open: boolean;
      caseIds: string[];  // 支持多选
      folders: TestCaseFolder[];
      currentFolderId: string | null;
      onClose: () => void;
      onSuccess: () => void;
    }
    ```
    UI：Dialog 内展示目录树（排除当前目录），用户选择目标目录。
    API：POST /testcases/folders/move-cases，body: {case_ids: caseIds, target_folder_id: targetId}。
    成功后 toast.success(`已移动 ${caseIds.length} 条用例`) + onSuccess() + onClose()。

    两个 Dialog 均使用：
    - shadcn/ui Dialog, Button, ScrollArea
    - sy-* 颜色 token（sy-bg-2 背景，sy-border 边框）
    - Loader2 animate-spin 加载状态
    - 目录列表用 FolderOpen/Folder from lucide-react 图标
  </action>
  <verify>
    <automated>cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "error TS.*(MoveFolderDialog|MoveCaseDialog)" | head -10</automated>
  </verify>
  <done>
    两个 Dialog 组件 TypeScript 无错误，MoveFolderDialog 可选目录并调用 PATCH API，MoveCaseDialog 批量移动并调用 move-cases API
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/poco/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit && bunx biome check src/app/\(main\)/testcases/_components/FolderTree.tsx src/app/\(main\)/testcases/_components/MoveFolderDialog.tsx src/app/\(main\)/testcases/_components/MoveCaseDialog.tsx
```
无 TypeScript 和 biome 错误。
</verification>

<success_criteria>
- FolderTree.tsx 有「移动到…」右键菜单项（is_system 目录不显示）
- 重命名重名校验生效（同级同名弹 toast.error）
- MoveFolderDialog 和 MoveCaseDialog 组件存在且 TypeScript 通过
- 移动 API 调用路径正确（PATCH folders/{id}、POST folders/move-cases）
</success_criteria>

<output>
After completion, create `.planning/phases/04-wai-wei-mo-kuai-kuo-zhan/04-05-SUMMARY.md`
</output>
