---
phase: 05-ti-yan-shou-wei
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - frontend/src/app/(main)/templates/page.tsx
  - frontend/src/app/(main)/templates/page.test.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
must_haves:
  truths:
    - "模板库 Prompt Tab显示 Prompt 列表"
    - "每张卡片显示模块名、类型（是否内置）"
    - "导入按钮触发文件上传并解析导入"
    - "导出 JSON 按钮下载新模板"
    - "内置模板显示内置 Badge"
    - "复制按钮复制后另存为自定义模板"
    - "查看详情按钮跳转设置页编辑"
  artifacts:
    - path: "frontend/src/app/(main)/templates/page.tsx"
      provides: "模板库页面"
      contains: "useTemplate"
    - path: "frontend/src/lib/api.ts"
      provides: "templatesApi"
      contains: "templatesApi"
  key_links:
    - from: "templates/page.tsx"
      to: "templatesApi.list()"
      via: "API call"
      pattern: "templatesApi"
---

<objective>
完成模板库 Prompt Tab 功能：展示 _MODULE_PROMPTS 字典中的 6 个模块，支持导出/导入 JSON，区分内置/自定义模板。

Purpose: 完善模板库功能，支持 Prompt 模板管理
Output: 模板库 Prompt Tab 完整功能
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
@.planning/phases/05-ti-yan-shou-wei/05-RESEARCH.md
@.planning/phases/05-ti-yan-shou-wei/05-VALIDATION.md

<interfaces>
From backend/app/ai/prompts.py:
```python
_MODULE_PROMPTS = {
    "diagnosis": {...},
    "scene_map": {...},
    "generation": {...},
    "diagnosis_followup": {...},
    "diff": {...},
    "exploratory": {...},
}

PROMPT_DISPLAY_NAMES = {
    "diagnosis": "需求诊断",
    "scene_map": "场景地图",
    "generation": "用例生成",
    "diagnosis_followup": "追问补充",
    "diff": "需求对比",
    "exploratory": "探索测试",
}
```

From frontend/src/app/(main)/templates/page.tsx:
```typescript
// 现有 Tab 结构
const [activeTab, setActiveTab] = useState<'case' | 'prompt'>('case')

// Prompt Tab 当前显示 "即将上线" 占位符
{activeTab === 'prompt' && (
  <div className="text-center py-16 text-sy-text-3">
    即将上线...
  </div>
)}
```

From frontend/src/lib/api.ts:
```typescript
export const templatesApi = {
  list: async (params?: { type?: string; pageSize?: number }) => {...},
  // 需要新增: listPrompts, exportPrompts, importPrompts
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Prompt Tab 列表展示</name>
  <files>
    frontend/src/app/(main)/templates/page.tsx
    frontend/src/lib/api.ts
  </files>
  <read_first>
    frontend/src/app/(main)/templates/page.tsx
    backend/app/ai/prompts.py
    .planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
  </read_first>
  <action>
1. 读取现有 templates/page.tsx 了解 Tab 结构

2. 在 api.ts 中添加 templatesApi.listPrompts 方法：
   ```typescript
   listPrompts: async () => {
     const response = await fetch('/api/settings/prompts')
     return response.json()
   }
   ```

3. 在 Prompt Tab 中实现列表展示：
   - 调用 templatesApi.listPrompts() 获取数据
   - 使用现有的卡片布局样式
   - 每张卡片显示：模块名（使用 PROMPT_DISPLAY_NAMES 映射）、是否内置
   - 内置模板显示 "内置" Badge（使用 bg-sy-accent/10 text-sy-accent 样式）

4. 添加搜索/筛选功能（复用现有 search state）

5. 添加操作按钮：
   - 查看详情：跳转 /settings?tab=prompts&module={module}
   - 复制：复制模板内容到剪贴板
   - 导出 JSON：下载单个模板 JSON 文件
</action>
  <verify>
    <automated>bun test frontend/src/app/(main)/templates/page.test.tsx 2>/dev/null || echo "Test file needs creation"</automated>
  </verify>
  <done>
    - Prompt Tab 显示 6 个模块的 Prompt 模板
    - 每张卡片显示模块名、是否内置
    - 内置模板显示 Badge
    - 搜索/筛选功能正常
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现导出/导入 JSON 功能</name>
  <files>
    frontend/src/app/(main)/templates/page.tsx
    frontend/src/lib/api.ts
  </files>
  <read_first>
    frontend/src/app/(main)/templates/page.tsx
    .planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
  </read_first>
  <action>
1. 实现导出功能：
   - 单个导出：点击卡片"导出 JSON"按钮，下载该模板的 JSON 文件
   - 批量导出：顶部"导出 JSON"按钮，下载所有模板的 JSON 文件
   - 文件名格式：`prompt-templates-{timestamp}.json`

2. 导出 JSON 结构：
   ```json
   {
     "version": "1.0",
     "exported_at": "2026-03-16T10:00:00Z",
     "templates": [
       { "module": "diagnosis", "content": "...", "is_builtin": true }
     ]
   }
   ```

3. 实现导入功能：
   - 顶部"导入"按钮触发文件选择
   - 使用 `<input type="file" accept=".json" />`
   - 解析 JSON 并验证结构
   - 调用 API 保存导入的模板

4. 导入验证：
   - 检查 JSON 结构有效性
   - 检查 module 字段是否在允许列表中
   - 显示导入结果 Toast（成功数量/失败原因）

5. 错误处理：
   - 文件格式错误：Toast 提示 "文件格式无效"
   - 解析失败：Toast 提示具体错误信息
</action>
  <verify>
    <automated>bun test frontend/src/app/(main)/templates/page.test.tsx 2>/dev/null || echo "Test file needs creation"</automated>
  </verify>
  <done>
    - 导出 JSON 功能可用，文件格式正确
    - 导入 JSON 功能可用，支持验证
    - 错误情况有 Toast 提示
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Prompt Tab 测试文件</name>
  <files>
    frontend/src/app/(main)/templates/page.test.tsx
  </files>
  <read_first>
    frontend/src/app/(main)/templates/page.tsx
    frontend/src/app/(main)/testcases/page.test.tsx
  </read_first>
  <action>
1. 创建测试文件 templates/page.test.tsx

2. 测试用例：
   - 渲染 Prompt Tab 时显示 6 个模块
   - 搜索功能过滤结果正确
   - 内置模板显示 Badge
   - 导出 JSON 生成正确文件
   - 导入有效 JSON 成功
   - 导入无效 JSON 显示错误 Toast
   - 查看详情按钮跳转正确路由

3. Mock API 响应：
   ```typescript
   vi.mock('@/lib/api', () => ({
     templatesApi: {
       listPrompts: vi.fn().mockResolvedValue({
         items: [
           { module: 'diagnosis', content: '...', is_builtin: true }
         ]
       })
     }
   }))
   ```
</action>
  <verify>
    <automated>bun test frontend/src/app/(main)/templates/page.test.tsx</automated>
  </verify>
  <done>
    - 测试文件创建完成
    - 所有测试用例通过
  </done>
</task>

</tasks>

<verification>
1. 运行前端测试: `bun test`
2. 访问模板库页面验证 Prompt Tab 列表展示正确
3. 测试导出/导入 JSON 功能
4. 验证内置 Badge 显示
</verification>

<success_criteria>
- 模板库 Prompt Tab 数据加载正常
- 模板导出/导入 JSON 功能可用
- 内置模板显示"内置" Badge
- 模板卡片显示模块名、类型和是否内置
- "查看详情"按钮跳转设置页编辑
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-ti-yan-shou-wei/05-03-SUMMARY.md`
</output>
