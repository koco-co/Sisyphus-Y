---
phase: 05-ti-yan-shou-wei
plan: 01
type: execute
wave: 1
depends_on: [05-00]
files_modified:
  - frontend/src/components/ui/ConfirmDialog.tsx
  - frontend/src/components/ui/ConfirmDialog.test.tsx
  - frontend/src/components/ui/EmptyState.tsx
  - frontend/src/components/ui/EmptyState.test.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
must_haves:
  truths:
    - "所有删除弹窗使用统一 ConfirmDialog 组件"
    - "简单删除显示「删除后可在回收站中找回」"
    - "级联删除显示影响数量 + 确认按钮为 sy-danger 色"
    - "空状态页面显示图标 48px + 暂无文案 + 引导操作按钮"
  artifacts:
    - path: "frontend/src/components/ui/ConfirmDialog.tsx"
      provides: "统一删除弹窗组件"
      exports: ["ConfirmDialog"]
    - path: "frontend/src/components/ui/EmptyState.tsx"
      provides: "空状态展示组件"
      exports: ["EmptyState"]
  key_links:
    - from: "各页面删除操作"
      to: "ConfirmDialog"
      via: "variant prop"
      pattern: "variant.*simple|cascade"
---

<objective>
扩展 ConfirmDialog 支持 simple/cascade 两种删除模式，确保 EmptyState 组件符合 48px 图标规范，并通过全局脚本扫描统一按钮文案。

Purpose: 统一全局 UI 规范，提升用户体验一致性
Output: 扩展后的 ConfirmDialog、验证过的 EmptyState、按钮文案统一
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
@.planning/phases/05-ti-yan-shou-wei/05-RESEARCH.md

<interfaces>
From frontend/src/components/ui/ConfirmDialog.tsx:
```typescript
interface ConfirmDialogProps {
  open: boolean;
  onConfirm: () => void;
  onCancel: () => void;
  title?: string;
  description?: string;
  confirmText?: string;
  cancelText?: string;
  variant?: 'danger' | 'warning' | 'default';  // 需扩展为 'simple' | 'cascade'
}
```

From frontend/src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon?: ReactNode;
  title?: string;
  description?: string;
  action?: ReactNode;
  className?: string;
}
// icon 默认 <Inbox className="w-12 h-12" /> 已满足 48px 需求
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ConfirmDialog 支持 simple/cascade 删除模式</name>
  <files>
    frontend/src/components/ui/ConfirmDialog.tsx
    frontend/src/components/ui/ConfirmDialog.test.tsx
  </files>
  <read_first>
    frontend/src/components/ui/ConfirmDialog.tsx
    frontend/src/components/ui/ConfirmDialog.test.tsx
    .planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: variant="simple" 时 description 包含「删除后可在回收站中找回」
    - Test 2: variant="cascade" 时 description 包含「将同时删除 N 条关联用例」
    - Test 3: variant="cascade" 时确认按钮使用 sy-danger 样式（bg-sy-danger）
    - Test 4: impactCount prop 传入时正确显示数量
  </behavior>
  <action>
**TDD 流程：先运行测试确认 RED，再实现代码使测试 GREEN**

1. 运行 Wave 0 创建的测试（预期失败）：
   ```bash
   bun test frontend/src/components/ui/ConfirmDialog.test.tsx
   ```

2. 读取现有 ConfirmDialog.tsx 了解当前结构

3. 扩展 variant 类型定义：
   ```typescript
   interface ConfirmDialogProps {
     // ...existing props
     variant?: 'danger' | 'warning' | 'default' | 'simple' | 'cascade';
     impactCount?: number;  // cascade 模式显示影响数量
     itemName?: string;     // 用于生成默认文案
   }
   ```

4. 实现 simple 模式逻辑：
   - 当 variant="simple" 时，如果 description 未传入，自动生成文案：
     `确定删除「${itemName}」？删除后可在回收站中找回。`

5. 实现 cascade 模式逻辑：
   - 当 variant="cascade" 时，如果 description 未传入，自动生成文案：
     `确定删除「${itemName}」？将同时删除 ${impactCount} 条关联用例。此操作不可撤销。`
   - 确认按钮样式使用 bg-sy-danger

6. 更新按钮样式逻辑：
   ```typescript
   const confirmClass =
     variant === 'danger' || variant === 'cascade'
       ? 'bg-sy-danger text-white hover:bg-sy-danger/90'
       : variant === 'warning'
         ? 'bg-sy-warn text-white hover:bg-sy-warn/90'
         : 'bg-sy-accent text-white hover:bg-sy-accent-2';
   ```

7. 运行测试确认 GREEN：
   ```bash
   bun test frontend/src/components/ui/ConfirmDialog.test.tsx
   ```

8. 提交代码：
   ```bash
   git add frontend/src/components/ui/ConfirmDialog.tsx frontend/src/components/ui/ConfirmDialog.test.tsx
   git commit -m "feat(ui): add simple/cascade variants to ConfirmDialog"
   ```
</action>
  <verify>
    <automated>bun test frontend/src/components/ui/ConfirmDialog.test.tsx</automated>
  </verify>
  <done>
    - ConfirmDialog.tsx 包含 variant='simple' | 'cascade' 类型定义
    - ConfirmDialog.tsx 包含 impactCount 和 itemName props
    - simple 模式显示「删除后可在回收站中找回」
    - cascade 模式显示影响数量 + 确认按钮为 sy-danger 色
    - 测试全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 验证 EmptyState 符合 48px 图标规范</name>
  <files>
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/EmptyState.test.tsx
  </files>
  <read_first>
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/EmptyState.test.tsx
    .planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: 默认渲染包含 w-12 h-12（48px）的图标
    - Test 2: title 默认值为「暂无数据」
    - Test 3: action 按钮正确渲染
  </behavior>
  <action>
**TDD 流程：先运行测试确认 RED，再验证/调整代码使测试 GREEN**

1. 运行 Wave 0 创建的测试（预期失败）：
   ```bash
   bun test frontend/src/components/ui/EmptyState.test.tsx
   ```

2. 读取 EmptyState.tsx 验证当前实现

3. 确认图标尺寸：
   - 当前默认图标应为: `<Inbox className="w-12 h-12" />`
   - w-12 h-12 = 48px，符合 UX-04 要求

4. 如需调整，确保：
   - 默认图标保持 w-12 h-12
   - 自定义 icon prop 传入时，由调用方控制尺寸

5. 运行测试确认 GREEN：
   ```bash
   bun test frontend/src/components/ui/EmptyState.test.tsx
   ```

6. 提交代码（如有修改）：
   ```bash
   git add frontend/src/components/ui/EmptyState.tsx frontend/src/components/ui/EmptyState.test.tsx
   git commit -m "fix(ui): ensure EmptyState icon is 48px"
   ```
</action>
  <verify>
    <automated>bun test frontend/src/components/ui/EmptyState.test.tsx</automated>
  </verify>
  <done>
    - EmptyState 默认图标尺寸为 w-12 h-12（48px）
    - 组件支持 title/description/action props
    - 测试全部通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 全局按钮文案统一「新建 + 对象名」</name>
  <files>
    frontend/src/app/(main)/testcases/page.tsx
    frontend/src/app/(main)/templates/page.tsx
    frontend/src/app/(main)/knowledge/page.tsx
  </files>
  <read_first>
    frontend/src/app/(main)/testcases/page.tsx
    frontend/src/app/(main)/templates/page.tsx
    frontend/src/app/(main)/knowledge/page.tsx
    .planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
  </read_first>
  <action>
1. 全局搜索「新增」「创建」关键词：
   ```bash
   grep -rn "新增\|创建" frontend/src/app/ --include="*.tsx" | grep -v "node_modules"
   ```

2. 替换规则（根据 CONTEXT.md 锁定决策）：
   - 「新增」→「新建」
   - 「创建」→「新建」
   - 保留例外：上传、添加、导入（不改为「新建」）
   - 格式：「新建 + 对象名」，如「新建需求」「新建用例」「新建模板」

3. 重点检查页面：
   - testcases/page.tsx: 检查用例相关按钮
   - templates/page.tsx: 已有「新建模板」，确认一致
   - knowledge/page.tsx: 检查知识库相关按钮

4. 验证方式：
   ```bash
   grep -rn "新增\|创建" frontend/src/app/ --include="*.tsx" | wc -l
   # 应返回 0 或仅剩保留例外
   ```

5. 提交代码：
   ```bash
   git add frontend/src/app/
   git commit -m "style: unify button text to '新建 + 对象名' format"
   ```
</action>
  <verify>
    <automated>grep -c "新增\|创建" frontend/src/app/(main)/testcases/page.tsx frontend/src/app/(main)/templates/page.tsx frontend/src/app/(main)/knowledge/page.tsx | grep -v ":0$" || echo "All replaced"</automated>
  </verify>
  <done>
    - 全局无「新增」「创建」按钮文案（保留例外除外）
    - 所有新建按钮格式为「新建 + 对象名」
  </done>
</task>

</tasks>

<verification>
1. 运行前端测试: `bun test`
2. 检查 ConfirmDialog simple/cascade 模式渲染
3. 检查 EmptyState 图标尺寸
4. 验证按钮文案统一
</verification>

<success_criteria>
- ConfirmDialog 支持 simple/cascade 两种删除模式
- EmptyState 图标尺寸为 48px
- 全局按钮文案统一为「新建 + 对象名」
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-ti-yan-shou-wei/05-01-SUMMARY.md`
</output>
