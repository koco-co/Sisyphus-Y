---
phase: 05-ti-yan-shou-wei
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - UX-07
  - UX-08
  - REC-01
  - REC-02
  - REC-03
  - TPL-01
  - TPL-02
  - TPL-03
must_haves:
  truths:
    - "所有加载状态符合规范（骨架屏/Loader2/nprogress）"
    - "OnboardingGuide 首次访问显示，关闭后不再显示"
    - "HelpFab 在右下角常驻，点击显示菜单"
    - "AiConfigBanner 在分析台/工作台/用例库正确显示"
    - "全局 UI 规范一致性验证通过"
  artifacts:
    - path: "frontend/src/components/ui/OnboardingGuide.tsx"
      provides: "新手引导组件"
      contains: "sisyphus-y-onboarding-seen"
    - path: "frontend/src/components/ui/HelpFab.tsx"
      provides: "帮助浮动按钮"
      contains: "fixed bottom-6 right-6"
    - path: "frontend/src/components/ui/AiConfigBanner.tsx"
      provides: "AI 配置提示 Banner"
      contains: "AiConfigBanner"
  key_links:
    - from: "各页面首次访问"
      to: "OnboardingGuide"
      via: "localStorage check"
      pattern: "sisyphus-y-onboarding-seen"
---
---

<objective>
Phase 5 最终验收：检查所有 UI 规范一致性，验证 OnboardingGuide、HelpFab 和 AiConfigBanner 功能，确认加载状态规范实现。

Purpose: 确保用户体验收尾工作完成，所有规范落地
Output: Phase 5 验证报告和 SUMMARY
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ti-yan-shou-wei/05-CONTEXT.md
@.planning/phases/05-ti-yan-shou-wei/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 所有功能：
1. ConfirmDialog simple/cascade 删除模式
2. EmptyState 48px 图标规范
3. 全局按钮文案「新建 + 对象名」
4. 回收站 cleanup API 和加载状态
5. 模板库 Prompt Tab 和 Markdown 导出/导入
6. OnboardingGuide 首次访问显示
7. HelpFab 帮助浮动按钮
8. AiConfigBanner AI 配置提示（分析台/工作台/用例库）
9. 全局加载状态规范（骨架屏/Loader2/nprogress）
  </what-built>
  <how-to-verify>
## 视觉和功能验证

### 1. 删除弹窗验证
- 访问 `/testcases`，删除单个用例
- 确认弹窗显示「删除后可在回收站中找回」
- 删除需求（有级联用例）
- 确认弹窗显示「将同时删除 N 条关联用例」
- 确认级联删除按钮为红色（bg-sy-danger）

### 2. 空状态验证
- 访问无数据的列表页面（如新建产品的需求列表）
- 确认图标尺寸为 48px（w-12 h-12）
- 确认显示「暂无数据」文案和新建按钮

### 3. 按钮文案验证
- 检查所有新建按钮文案为「新建 + 对象名」格式
- 确认无「新增」「创建」文案（保留例外除外）

### 4. 回收站验证
- 访问 `/recycle`
- 确认首次加载显示骨架屏
- 确认即将过期（<=3天）项目显示红色
- 测试恢复功能，确认 Toast 提示
- 切换 Tab 筛选，确认过滤正确

### 5. 模板库验证
- 访问 `/templates`，切换到 Prompt Tab
- 确认显示 6 个模块的 Prompt 模板
- 确认内置模板显示「内置」Badge
- 测试导出 Markdown 功能
- 测试导入 Markdown 功能

### 6. 首次引导验证
- 打开 DevTools → Application → Local Storage
- 删除 `sisyphus-y-onboarding-seen` 键
- 刷新首页
- 确认显示新手引导弹窗
- 完成引导后刷新，确认不再显示

### 7. 帮助按钮验证
- 确认右下角有圆形帮助按钮（sy-accent 背景）
- 点击帮助按钮
- 确认显示菜单，包含「重新查看引导」「快捷键」「反馈问题」
- 点击「重新查看引导」，确认引导弹窗重新显示

### 8. AI 配置警告验证
- 确保未配置 AI（设置页清空 API Key）
- 访问 `/analysis`
- 确认顶部显示黄色警告横幅，包含「前往配置」链接
- 访问 `/workbench`
- 确认顶部显示同样警告横幅
- 访问 `/testcases`
- 确认顶部显示同样警告横幅

### 9. 加载状态验证
- 刷新各列表页面，确认首次加载显示骨架屏
- 点击各操作按钮，确认显示 Loader2 动画
- 切换路由，确认 nprogress 进度条
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 运行完整测试套件</name>
  <files></files>
  <action>
1. 运行前端测试：
   ```bash
   cd frontend && bun test
   ```

2. 运行后端测试：
   ```bash
   cd backend && uv run pytest -v
   ```

3. 运行 lint 和类型检查：
   ```bash
   # 前端
   cd frontend && bunx biome check . && bunx tsc --noEmit

   # 后端
   cd backend && uv run ruff check . && uv run pyright app/
   ```

4. 汇总测试结果
</action>
  <verify>
    <automated>bun test && uv run pytest -v</automated>
  </verify>
  <done>
    - 所有前端测试通过
    - 所有后端测试通过
    - Lint 和类型检查通过
  </done>
</task>

</tasks>

<verification>
1. 人工验证所有 UI 规范
2. 运行完整测试套件
3. 确认无 console.log 残留
4. 确认无硬编码色值
</verification>

<success_criteria>
- 所有删除弹窗符合 simple/cascade 规范
- EmptyState 图标 48px 符合规范
- 加载状态规范: 骨架屏/Loader2, 按钮 Loader2, 路由跳转 nprogress
- 回收站清理功能正常工作
- 模板库 Prompt Tab 数据加载正常
- 模板导出/导入 **Markdown** 功能可用
- 内置模板显示「内置」Badge
- 首次访问显示引导弹窗，关闭后不再显示
- 帮助按钮在右下角常驻，点击显示菜单
- AI 未配置时分析台/工作台/用例库显示警告横幅
- 所有测试通过
- 用户体验无明显缺陷
</success_criteria>

<output>
After completion, create `.planning/phases/05-ti-yan-shou-wei/05-05-SUMMARY.md`
</output>
