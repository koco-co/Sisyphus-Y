---
phase: 03-ai
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ai/prompts.py
  - frontend/src/app/(main)/workbench/_components/ChatArea.tsx
  - backend/tests/unit/test_ai/test_prompts.py
autonomous: true
requirements:
  - RAG-06
  - PRM-01
  - PRM-02
  - PRM-03
  - PRM-04

must_haves:
  truths:
    - "6 个模块 Prompt 均包含四段式结构标头（① 身份声明 / ② 任务边界 / ③ 输出规范 / ④ 质量红线）"
    - "5 个模块（DIAGNOSIS, SCENE_MAP, DIAGNOSIS_FOLLOWUP, DIFF_SEMANTIC, EXPLORATORY）各含 2~3 正例 + 1 负例的 Few-shot 段落"
    - "6 个模块身份声明中的职责描述互不相同（无同质化短语）"
    - "工作台 Step2 流式输出中换行符正确渲染为换行（renderMarkdown 已处理 \\n → <br/>）"
    - "backend/app/core/config.py 中 zhipu_model 默认值为 'glm-5'"
  artifacts:
    - path: "backend/app/ai/prompts.py"
      provides: "6 个模块 System Prompt，含 Few-shot 正负例"
    - path: "frontend/src/app/(main)/workbench/_components/ChatArea.tsx"
      provides: "SSE 换行渲染修复（renderMarkdown \\n 处理确认）"
    - path: "backend/tests/unit/test_ai/test_prompts.py"
      provides: "PRM-02 身份声明差异化测试 + PRM-03 Few-shot 存在性测试"
  key_links:
    - from: "backend/app/ai/prompts.py"
      to: "backend/app/engine/"
      via: "assemble_prompt(module=...) → Layer 1 注入 Module System Prompt"
      pattern: "assemble_prompt"
    - from: "ChatArea.tsx renderMarkdown"
      to: "streamingContent display"
      via: "dangerouslySetInnerHTML={{ __html: renderMarkdown(streamingContent) }}"
      pattern: "renderMarkdown"
---

<objective>
重写 5 个尚未有 Few-shot 的模块 Prompt（GENERATION_SYSTEM 已有完整示例），修复 SSE 换行渲染 bug，并验证 GLM-5 配置。

Purpose: 让所有 AI 模块使用更精准的差异化身份声明和 Few-shot 示例，提升输出结构稳定性；修复前端 \\n 渲染 bug 确保用例步骤格式正确显示。
Output: 重写后的 prompts.py，SSE 渲染确认/修复，Prompt 测试用例
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-RESEARCH.md
</context>

<interfaces>
<!-- 当前 prompts.py 中 6 个模块 Prompt 现状 -->

已有完整 Few-shot（标准，其他模块参照）：
- GENERATION_SYSTEM（约 230 行，3 正例 + 2 负例，覆盖 normal/exception/boundary/precondition）

需要补 Few-shot 的 5 个模块：
- DIAGNOSIS_SYSTEM — 需求质量分析专家，输出 JSON {overall_health_score, summary, dimensions[]}
- SCENE_MAP_SYSTEM — 测试场景地图构建专家，输出 JSON 数组（测试点列表）
- DIAGNOSIS_FOLLOWUP_SYSTEM — 需求深度追问专家，每次只问一个问题
- DIFF_SEMANTIC_SYSTEM — 测试影响分析专家，输出 {summary, overall_impact, change_points[]}
- EXPLORATORY_SYSTEM — 对话式 AI 测试助手，自然语言回复 + 用例 JSON

PRM-04 状态：
- backend/app/core/config.py: zhipu_model: str = "glm-5"（已是目标值）
- frontend/src/app/(main)/settings/_components/AIModelSettings.tsx: 默认 modelId 已是 glm-5
- 实际工作仅需测试确认（pytest test_core/ -k config）

SSE 换行现状：
- renderMarkdown() 已有 .replace(/\n/g, '<br/>') 处理
- 流式输出路径：streamingContent → renderMarkdown → dangerouslySetInnerHTML（已有 \\n→<br/>）
- 历史消息路径：message.content → renderMarkdown → dangerouslySetInnerHTML（第 235 行，已覆盖）
- 唯一遗漏路径（第 255 行）：`<span className="whitespace-pre-wrap">{message.content}</span>`
  这是非 AI 消息（用户侧 fallback）或特定条件下显示 raw content 的路径，需确认并加 renderMarkdown
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补充 test_prompts.py 中的 identity 和 fewshot 测试</name>
  <files>backend/tests/unit/test_ai/test_prompts.py</files>
  <behavior>
    新增以下测试（追加到已有文件，不删除现有测试）：

    test_identity_unique:
    - 取 6 个 prompt 常量（DIAGNOSIS_SYSTEM, SCENE_MAP_SYSTEM, GENERATION_SYSTEM,
      DIAGNOSIS_FOLLOWUP_SYSTEM, DIFF_SEMANTIC_SYSTEM, EXPLORATORY_SYSTEM）
    - 提取每个 "## ① 身份声明" 段落首句
    - 断言 6 个首句两两不同（无重复）
    - 断言每个身份声明长度 > 30 字（非空泛描述）

    test_fewshot_present:
    - 断言 GENERATION_SYSTEM 包含 "Few-Shot" 或 "Few-shot" 或 "正例"
    - 断言以下 5 个模块也包含至少一个 "正例" 或 "✅" 标记：
      DIAGNOSIS_SYSTEM, SCENE_MAP_SYSTEM, DIAGNOSIS_FOLLOWUP_SYSTEM,
      DIFF_SEMANTIC_SYSTEM, EXPLORATORY_SYSTEM
    - 断言每个含 Few-shot 的 prompt 也包含 "❌" 或 "负例"（至少一个负例）

    test_four_section_structure:
    - 对所有 6 个 prompt 断言包含 "## ① 身份声明", "## ② 任务边界",
      "## ③ 输出规范", "## ④ 质量红线" 四个段落标头

    test_glm5_config:
    - 从 app.core.config import Settings
    - 断言 Settings().zhipu_model == "glm-5" 或 Settings 的默认字段值为 "glm-5"
  </behavior>
  <action>
    先读取 backend/tests/unit/test_ai/test_prompts.py 了解现有结构，然后追加新测试。
    先运行确认 RED（因为 prompts.py 中 5 个模块还没有 Few-shot）：
    `cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_ai/test_prompts.py -k "identity or fewshot or four_section" -x -q`
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_ai/test_prompts.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>测试文件更新完毕，新增的 identity/fewshot/four_section/glm5_config 测试可被 pytest 收集（RED 状态可接受）</done>
</task>

<task type="auto">
  <name>Task 2: 重写 5 个模块 Prompt + 修复 SSE 换行 + 令测试 GREEN</name>
  <files>
    backend/app/ai/prompts.py
    frontend/src/app/(main)/workbench/_components/ChatArea.tsx
  </files>
  <action>
    ### 步骤一：重写 prompts.py 中 5 个模块的 Few-shot 段落

    在每个模块现有四段式结构后（## ④ 质量红线 之后）追加 ## ⑤ Few-Shot 示例 段落。
    参照 GENERATION_SYSTEM 的格式风格，每模块 2~3 正例 + 1 负例。
    正例从历史数据场景中精选（步骤数 ≤ 4 的简洁案例），负例手动编写（典型低质量模式）。

    **DIAGNOSIS_SYSTEM Few-shot 结构**（需求质量分析）：
    - 正例 1 — 高风险（high）：权限体系缺少数据隔离描述，risk_level: "high"，引用需求原文
    - 正例 2 — 中风险（medium）：边界值未说明最大记录数，risk_level: "medium"
    - 负例 1 — 模糊 description（❌ 不引用原文，只说"需求不完整"）
    JSON 格式与现有 ③ 输出规范完全一致（overall_health_score, dimensions[]）

    **SCENE_MAP_SYSTEM Few-shot 结构**（场景地图）：
    - 正例 1 — normal：登录主流程测试点，estimated_cases: 3
    - 正例 2 — exception：批量导入异常场景，estimated_cases: 4
    - 负例 1 — ❌ estimated_cases 超出范围（写 8）且 title 过长
    JSON 格式与现有 ③ 输出规范一致（group_name, title, scenario_type, priority, estimated_cases）

    **DIAGNOSIS_FOLLOWUP_SYSTEM Few-shot 结构**（深度追问）：
    - 正例 1 — 针对"数据同步失败"追问具体重试机制：单问句，≤ 80 字
    - 正例 2 — 追问总结阶段输出格式：以 "📋 追问总结" 开头，清单结构
    - 负例 1 — ❌ 一次提出 3 个问题（违反"每次只问一个"）

    **DIFF_SEMANTIC_SYSTEM Few-shot 结构**（变更影响分析）：
    - 正例 1 — needs_rewrite：接口参数类型从 string→int，JSON 格式完整
    - 正例 2 — needs_review：超时时间从 30s→10s，只影响边界值用例
    - 负例 1 — ❌ 实质业务变更但 overall_impact 判为 no_impact

    **EXPLORATORY_SYSTEM Few-shot 结构**（对话式助手）：
    - 正例 1 — 用户"测试登录超时"，AI 先简短说明再输出 1 条用例 JSON（--- 分隔）
    - 正例 2 — 用户反馈"再加一个边界场景"，AI 追加生成 1 条 boundary 用例
    - 负例 1 — ❌ 用户只要求"补充异常"，AI 重新生成全套 5 条用例（违反"用户明确要求时仅修改该用例"）

    注意：
    - 每个正例用 ### ✅ 正例 N — 场景类型 标头，与 GENERATION_SYSTEM 格式一致
    - 每个负例用 ### ❌ 负例 N — 问题描述 标头，附 **正确做法** 说明
    - 正例步骤数保持 ≤ 4（避免 Prompt 膨胀），precondition 非空且具体

    ### 步骤二：确认 SSE 换行渲染

    ChatArea.tsx 第 255 行：`<span className="whitespace-pre-wrap">{message.content}</span>`
    这是 `showRawContent && !isAI` 路径（用户消息 fallback）。
    已有 renderMarkdown 处理流式内容（第 371 行）和历史 AI 消息（第 235 行）。

    检查第 255 行的条件分支（读 ChatArea.tsx 第 178-260 行）：
    - 如果这行渲染的是 AI 消息的 raw content（当 contentHasJson=false 且 showRawContent=true 但没有 JSON），
      且来自历史消息（非流式），需要换为 renderMarkdown
    - 如果这行只处理用户消息（isAI=false），whitespace-pre-wrap 已够用

    修复方案：若第 255 行在 isAI=true 路径下出现，改为：
    ```tsx
    dangerouslySetInnerHTML={{ __html: renderMarkdown(message.content) }}
    ```
    若只在 isAI=false 路径，维持 whitespace-pre-wrap 不变（用户消息无 \\n 渲染问题）。

    ### 步骤三：运行测试确认 GREEN

    `cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_ai/test_prompts.py -x -q`
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_ai/test_prompts.py -x -q</automated>
  </verify>
  <done>
    - test_identity_unique 通过（6 个身份声明互不相同）
    - test_fewshot_present 通过（5 个模块含正例 + 负例）
    - test_four_section_structure 通过（所有模块有四段式标头）
    - test_glm5_config 通过（zhipu_model 默认 glm-5）
    - ChatArea.tsx 中 AI 消息路径的 \\n 通过 renderMarkdown 转为 &lt;br/&gt;（渲染正确）
    - `bunx tsc --noEmit` 无新增 TS 错误（若修改了 ChatArea.tsx）
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend
uv run pytest tests/unit/test_ai/test_prompts.py -x -q
```
所有测试通过。

```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend
uv run pytest tests/unit/ -q 2>&1 | tail -5
```
全局测试套件无回归。
</verification>

<success_criteria>
- prompts.py 中 5 个模块均有 ## ⑤ Few-Shot 示例 段落（2~3 正例 + 1 负例）
- test_prompts.py 新增测试全部 GREEN
- ChatArea.tsx 中 AI 历史消息内容通过 renderMarkdown 渲染（\\n → &lt;br/&gt;）
- config.py zhipu_model = "glm-5" 已验证（PRM-04）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-ai/03-02-SUMMARY.md`，记录：
- 5 个模块 Few-shot 补充的场景选择说明
- SSE 换行 bug 的实际定位和修复范围
- GLM-5 配置验证结果
</output>
