---
phase: 03-ai
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_rag/test_review_script.py
  - backend/tests/unit/test_rag/test_review_rules.py
  - backend/tests/unit/test_rag/test_review_verdict.py
  - backend/tests/unit/test_rag/test_review_report.py
  - backend/tests/unit/test_rag/test_csv_fields.py
  - backend/tests/unit/test_rag/test_recreate.py
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04
  - RAG-07
  - RAG-08
must_haves:
  truths:
    - "审查脚本可以正确读取 CSV 文件（含 BOM）"
    - "审查规则在 LLM Prompt 中完整定义"
    - "三分支决策逻辑（pass/polish/discard）正确实现"
    - "审查报告包含总数/通过/润色/丢弃/丢弃原因汇总"
    - "向量库清空后可正确重建"
  artifacts:
    - path: "backend/tests/unit/test_rag/test_review_script.py"
      provides: "RAG-01 CSV 读取 + LLM mock 调用测试"
      exports: ["TestReviewScript"]
    - path: "backend/tests/unit/test_rag/test_review_rules.py"
      provides: "RAG-02 审查规则 Prompt 文本验证"
      exports: ["TestReviewRules"]
    - path: "backend/tests/unit/test_rag/test_review_verdict.py"
      provides: "RAG-02/03 判断逻辑 + 三分支决策测试"
      exports: ["TestReviewVerdict"]
    - path: "backend/tests/unit/test_rag/test_review_report.py"
      provides: "RAG-04 报告 JSON 格式验证"
      exports: ["TestReviewReport"]
    - path: "backend/tests/unit/test_rag/test_csv_fields.py"
      provides: "RAG-07 CSV BOM 解析 + 字段名中文验证"
      exports: ["TestCsvFields"]
    - path: "backend/tests/unit/test_rag/test_recreate.py"
      provides: "RAG-08 collection 清空 + 重建测试"
      exports: ["TestRecreateCollection"]
  key_links:
    - from: "scripts/review_testcases.py"
      to: "backend/tests/unit/test_rag/test_review_script.py"
      via: "unit test coverage"
      pattern: "read_csv_cases|review_case|REVIEW_SYSTEM"
prerequisites:
  - path: "scripts/review_testcases.py"
    description: "历史用例审查脚本（已存在于代码库中，本计划编写测试覆盖其功能）"
---

<objective>
验证历史用例审查脚本的核心功能通过单元测试覆盖。

Purpose: 确保审查脚本的 CSV 读取、LLM 调用、三分支决策、报告生成和向量库重建逻辑都有对应的自动化测试。
Output: 6 个测试文件，覆盖 RAG-01/02/03/04/07/08 需求。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-RESEARCH.md

<interfaces>
<!-- 关键接口，executor 需要实现测试覆盖 -->
<!-- 注意：scripts/review_testcases.py 是已存在的脚本，本计划编写测试覆盖其功能 -->

From scripts/review_testcases.py:
```python
def read_csv_cases(path: str, sample: int | None = None) -> list[dict]
def make_point_id(file_path: str, row_index: int) -> str
def csv_row_to_text(row: dict) -> str
def _extract_json(content: str) -> dict | None
async def review_case(case_text: str) -> dict
def generate_report(review_results: list[dict]) -> dict
async def rebuild_knowledge_chunks() -> int
```

From backend/app/engine/rag/retriever.py:
```python
def recreate_collection(*, collection_name: str = COLLECTION_NAME, vector_size: int = EMBEDDING_DIMENSION) -> dict
TESTCASE_COLLECTION = "historical_testcases"
COLLECTION_NAME = "knowledge_chunks"
```

From backend/app/ai/prompts.py:
```python
# 审查规则定义（在 REVIEW_SYSTEM 中）
REVIEW_SYSTEM = """..."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 CSV 读取测试覆盖（RAG-01, RAG-07）</name>
  <files>backend/tests/unit/test_rag/test_csv_fields.py, backend/tests/unit/test_rag/test_review_script.py</files>
  <read_first>
    - scripts/review_testcases.py
    - backend/tests/unit/test_rag/test_csv_fields.py
    - backend/tests/unit/test_rag/test_review_script.py
  </read_first>
  <behavior>
    - Test 1: CSV 文件带 BOM 前缀时能正确解析字段名
    - Test 2: CSV 字段名为中文（用例标题/前置条件/步骤/预期结果/优先级）
    - Test 3: read_csv_cases 支持 sample 参数限制读取行数
    - Test 4: csv_row_to_text 正确格式化中文标签
  </behavior>
  <action>
1. 读取现有测试文件 `backend/tests/unit/test_rag/test_csv_fields.py` 和 `test_review_script.py`
2. 确认测试覆盖以下场景：
   - `test_csv_with_bom_prefix`: 使用 `utf-8-sig` 编码读取含 `\ufeff` BOM 的 CSV
   - `test_csv_chinese_field_names`: 验证字段名包含中文（用例标题、步骤等）
   - `test_read_csv_cases_sample_limit`: 验证 sample 参数限制行数
   - `test_csv_row_to_text_chinese_labels`: 验证输出文本包含中文标签（如"用例:"、"步骤:"）
3. 如测试缺失或不完整，补充测试用例
4. 确保所有测试使用 mock 数据，不依赖真实文件
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_csv_fields.py tests/unit/test_rag/test_review_script.py -v --tb=short</automated>
  </verify>
  <done>
    - test_csv_fields.py 包含至少 2 个测试（BOM 解析 + 中文验证）
    - test_review_script.py 包含 CSV 读取相关测试
    - 所有测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证审查规则测试覆盖（RAG-02）</name>
  <files>backend/tests/unit/test_rag/test_review_rules.py</files>
  <read_first>
    - scripts/review_testcases.py（REVIEW_SYSTEM 常量）
    - backend/tests/unit/test_rag/test_review_rules.py
  </read_first>
  <behavior>
    - Test 1: REVIEW_SYSTEM 包含"第一步"相关规则描述
    - Test 2: REVIEW_SYSTEM 包含"预期结果"相关规则
    - Test 3: REVIEW_SYSTEM 包含"前置条件"相关规则
    - Test 4: REVIEW_SYSTEM 定义三种 verdict（pass/polish/discard）
  </behavior>
  <action>
1. 读取现有 `test_review_rules.py` 文件
2. 确认测试覆盖 REVIEW_SYSTEM 中的关键规则：
   - `test_review_system_contains_step_rule`: 验证包含"步骤"相关规则
   - `test_review_system_contains_expected_result_rule`: 验证包含"预期结果"规则
   - `test_review_system_contains_precondition_rule`: 验证包含"前置条件"规则
   - `test_review_system_verdict_values`: 验证 verdict 为 pass/polish/discard
   - `test_review_system_discard_reasons`: 验证丢弃原因分类（步骤缺失/预期缺失/无法修复/重复用例/无效用例）
3. 补充缺失的测试用例
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_review_rules.py -v --tb=short</automated>
  </verify>
  <done>
    - test_review_rules.py 包含至少 5 个测试
    - 所有测试验证 REVIEW_SYSTEM 的规则定义完整性
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证三分支决策测试覆盖（RAG-03）</name>
  <files>backend/tests/unit/test_rag/test_review_verdict.py</files>
  <read_first>
    - scripts/review_testcases.py（review_case 函数）
    - backend/tests/unit/test_rag/test_review_verdict.py
  </read_first>
  <behavior>
    - Test 1: LLM 返回 pass 时 verdict 为 pass
    - Test 2: LLM 返回 polish 时 verdict 为 polish 且包含 polished 字段
    - Test 3: LLM 返回 discard 时 verdict 为 discard 且包含 discard_reason
    - Test 4: LLM 返回格式异常时 verdict 为 discard 且 reason 为"无法修复"
  </behavior>
  <action>
1. 读取现有 `test_review_verdict.py` 文件
2. 确认测试覆盖 review_case 函数的三分支决策逻辑：
   - `test_verdict_pass`: mock LLM 返回 `{"verdict": "pass"}`，验证返回 pass
   - `test_verdict_polish`: mock LLM 返回 `{"verdict": "polish", "polished": {...}}`，验证返回 polish + polished 字段
   - `test_verdict_discard`: mock LLM 返回 `{"verdict": "discard", "discard_reason": "步骤缺失"}`，验证返回 discard + reason
   - `test_verdict_invalid_json`: mock LLM 返回非 JSON 内容，验证 fallback 为 discard + "无法修复"
3. 使用 `unittest.mock.patch` mock `invoke_llm` 调用
4. 补充缺失的测试用例
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_review_verdict.py -v --tb=short</automated>
  </verify>
  <done>
    - test_review_verdict.py 包含至少 4 个测试（pass/polish/discard/invalid）
    - 所有测试使用 mock LLM，不依赖真实 API
  </done>
</task>

<task type="auto">
  <name>Task 4: 验证审查报告测试覆盖（RAG-04）</name>
  <files>backend/tests/unit/test_rag/test_review_report.py</files>
  <read_first>
    - scripts/review_testcases.py（generate_report 函数）
    - backend/tests/unit/test_rag/test_review_report.py
  </read_first>
  <behavior>
    - Test 1: generate_report 返回 total/passed/polished/discarded 字段
    - Test 2: generate_report 统计 discard_reasons 频次
    - Test 3: 报告格式可序列化为 JSON
  </behavior>
  <action>
1. 读取现有 `test_review_report.py` 文件
2. 确认测试覆盖 generate_report 函数：
   - `test_report_structure`: 验证报告包含 total/passed/polished/discarded/discard_reasons 字段
   - `test_report_count_accuracy`: 给定 3 pass + 2 polish + 1 discard，验证计数正确
   - `test_report_discard_reasons_grouping`: 验证丢弃原因按类型分组计数
   - `test_report_json_serializable`: 验证报告可 json.dumps 序列化
3. 补充缺失的测试用例
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_review_report.py -v --tb=short</automated>
  </verify>
  <done>
    - test_review_report.py 包含至少 4 个测试
    - 报告结构验证通过
  </done>
</task>

<task type="auto">
  <name>Task 5: 验证向量库重建测试覆盖（RAG-08）</name>
  <files>backend/tests/unit/test_rag/test_recreate.py</files>
  <read_first>
    - backend/app/engine/rag/retriever.py（recreate_collection 函数）
    - backend/tests/unit/test_rag/test_recreate.py
  </read_first>
  <behavior>
    - Test 1: recreate_collection 先删除旧 collection 再创建新的
    - Test 2: recreate_collection 返回删除的 points 数量
    - Test 3: recreate_collection 对不存在的 collection 也能正常处理
  </behavior>
  <action>
1. 读取现有 `test_recreate.py` 文件
2. 确认测试覆盖 recreate_collection 函数：
   - `test_recreate_existing_collection`: 验证先删除后创建
   - `test_recreate_returns_deleted_count`: 验证返回 deleted_points 数量
   - `test_recreate_nonexistent_collection`: collection 不存在时不报错
3. 使用 mock QdrantClient
4. 补充缺失的测试用例
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_recreate.py -v --tb=short</automated>
  </verify>
  <done>
    - test_recreate.py 包含至少 3 个测试
    - 所有测试使用 mock，不依赖真实 Qdrant
  </done>
</task>

</tasks>

<verification>
执行所有 RAG 测试：
```bash
cd backend && uv run pytest tests/unit/test_rag/ -v --tb=short
```

预期结果：所有测试通过，覆盖 RAG-01/02/03/04/07/08 需求。
</verification>

<success_criteria>
- [ ] test_csv_fields.py 包含 BOM 解析和中文验证测试
- [ ] test_review_script.py 包含 CSV 读取测试
- [ ] test_review_rules.py 包含 5+ 审查规则测试
- [ ] test_review_verdict.py 包含三分支决策测试
- [ ] test_review_report.py 包含报告格式测试
- [ ] test_recreate.py 包含向量库重建测试
- [ ] 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai/03-01-SUMMARY.md`
</output>
