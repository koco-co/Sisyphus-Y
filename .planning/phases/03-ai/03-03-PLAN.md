---
phase: 03-ai
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/unit/test_rag/test_retriever.py
  - frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx
autonomous: true
requirements:
  - RAG-05
must_haves:
  truths:
    - "工作台 Step1 右栏 RAG 历史用例预览显示 top-5 结果"
    - "每条检索结果附相似度分数"
    - "相似度分数 >= 0.72"
  artifacts:
    - path: "backend/tests/unit/test_rag/test_retriever.py"
      provides: "RAG-05 retrieve_similar_cases 测试"
      exports: ["TestRetrieveSimilarCases"]
    - path: "frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx"
      provides: "RAG 预览 UI"
      exports: ["RagPreviewPanel"]
    - path: "backend/app/engine/rag/retriever.py"
      provides: "retrieve_similar_cases 函数"
      exports: ["retrieve_similar_cases", "retrieve_cases_as_context"]
  key_links:
    - from: "frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx"
      to: "backend/app/modules/scene-map/router.py"
      via: "POST /scene-map/{id}/rag-preview"
      pattern: "rag-preview"
    - from: "backend/app/modules/scene-map/router.py"
      to: "backend/app/engine/rag/retriever.py"
      via: "retrieve_similar_cases"
      pattern: "retrieve_similar_cases"
---

<objective>
验证 RAG 检索参数配置和端到端集成。

Purpose: 确保工作台 Step1 右栏的 RAG 预览功能正确显示 top-5 结果，每条附相似度分数，且分数 >= 0.72。
Output: 验证测试通过，确认前端 UI 正确调用后端 API。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-RESEARCH.md

<interfaces>
<!-- RAG 检索接口 -->

From backend/app/engine/rag/retriever.py:
```python
async def retrieve_similar_cases(
    query: str,
    *,
    top_k: int = 5,
    score_threshold: float = 0.72,
    product: str | None = None,
) -> list[RetrievalResult]:
    """从 historical_testcases collection 检索相似的历史用例。"""
```

From backend/app/modules/scene-map/router.py:
```python
@router.post("/{req_id}/rag-preview")
async def rag_preview(req_id: str, body: RagPreviewRequest):
    """返回 top-5 历史用例，每个结果附带相似度分数"""
```

From frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx:
```typescript
interface RagResult {
  title: string;
  score: number;
  content: string;
}
// 调用 POST /scene-map/${requirementId}/rag-preview
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 RAG 检索测试覆盖（RAG-05）</name>
  <files>backend/tests/unit/test_rag/test_retriever.py</files>
  <read_first>
    - backend/app/engine/rag/retriever.py
    - backend/tests/unit/test_rag/test_retriever.py
  </read_first>
  <behavior>
    - Test 1: retrieve_similar_cases 默认 top_k=5
    - Test 2: retrieve_similar_cases 默认 score_threshold=0.72
    - Test 3: 检索结果包含 score 字段
    - Test 4: 检索结果包含 metadata 字段
    - Test 5: collection 不存在时返回空列表
  </behavior>
  <action>
1. 读取现有 `test_retriever.py` 文件
2. 确认 TestRetrieveSimilarCases 类包含以下测试：
   - `test_retrieve_similar_cases_default_params`: 验证默认 top_k=5, score_threshold=0.72
   - `test_retrieve_similar_cases_includes_score`: 验证结果包含 score 字段
   - `test_retrieve_similar_cases_includes_metadata`: 验证结果包含 metadata
   - `test_retrieve_similar_cases_empty_on_missing_collection`: collection 不存在时返回 []
3. 使用 mock QdrantClient 和 mock embed_query
4. 补充缺失的测试用例
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_rag/test_retriever.py::TestRetrieveSimilarCases -v --tb=short</automated>
  </verify>
  <done>
    - TestRetrieveSimilarCases 包含至少 4 个测试
    - 验证默认 top_k=5, score_threshold=0.72 参数
    - 所有测试使用 mock，不依赖真实 Qdrant
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 RAG 预览 UI 显示 top-5 结果</name>
  <files>frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx</files>
  <read_first>
    - frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx
    - frontend/src/app/(main)/workbench/_components/RagPreviewPanel.test.tsx (如果存在)
  </read_first>
  <behavior>
    - RagPreviewPanel 显示最多 5 条结果
    - 每条结果显示 title、score 和 content
    - score >= 0.8 时使用绿色样式
    - score >= 0.6 时使用黄色样式
    - score < 0.6 时使用灰色样式
  </behavior>
  <action>
1. 读取 RagPreviewPanel.tsx
2. 验证 `ragResults.slice(0, 5)` 限制显示数量为 5
3. 验证 score 显示逻辑：
   - `result.score >= 0.8`: `bg-sy-accent/10 text-sy-accent`
   - `result.score >= 0.6`: `bg-amber/10 text-amber`
   - `result.score < 0.6`: `bg-sy-bg-3 text-sy-text-3`
4. 如果测试文件存在，运行测试
  </action>
  <verify>
    <automated>grep -n "slice(0, 5)" frontend/src/app/(main)/workbench/_components/RagPreviewPanel.tsx</automated>
  </verify>
  <done>
    - RagPreviewPanel 限制显示 top-5 结果
    - score 样式根据阈值正确显示
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证后端 RAG 预览端点调用 retrieve_similar_cases</name>
  <files>backend/tests/unit/test_rag/test_retriever.py</files>
  <read_first>
    - backend/app/modules/scene-map/router.py
    - backend/app/engine/rag/retriever.py
  </read_first>
  <behavior>
    - 后端 /scene-map/{id}/rag-preview 端点调用 retrieve_similar_cases
    - 调用时传递 top_k=5, score_threshold=0.72
  </behavior>
  <action>
1. 读取 scene-map router.py 中的 rag_preview 端点
2. 验证端点调用 `retrieve_similar_cases(query, top_k=5, score_threshold=0.72)`
3. 如果没有显式传参，确认默认值
4. 补充集成测试（如果缺失）
  </action>
  <verify>
    <automated>grep -n "retrieve_similar_cases" backend/app/modules/scene-map/router.py | head -20</automated>
  </verify>
  <done>
    - 后端端点正确调用 retrieve_similar_cases
    - 参数传递正确（top_k=5, score_threshold=0.72）
  </done>
</task>

</tasks>

<verification>
执行 RAG 检索相关测试：
```bash
cd backend && uv run pytest tests/unit/test_rag/test_retriever.py -v --tb=short
```

预期结果：
- TestRetrieveSimilarCases 测试通过
- 默认参数验证通过（top_k=5, score_threshold=0.72）
</verification>

<success_criteria>
- [ ] test_retriever.py 包含 TestRetrieveSimilarCases 类
- [ ] 测试验证 top_k=5 默认值
- [ ] 测试验证 score_threshold=0.72 默认值
- [ ] RagPreviewPanel 限制显示 5 条结果
- [ ] 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai/03-03-SUMMARY.md`
</output>
