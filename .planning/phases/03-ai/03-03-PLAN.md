---
phase: 03-ai
plan: "03-03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - backend/app/engine/rag/retriever.py
  - backend/app/modules/generation/router.py
autonomous: false
requirements:
  - RAG-05

must_haves:
  truths:
    - "工作台 Step1 右栏 RAG 历史用例预览显示 top-5 结果，每条附相似度分数"
    - "相似度分数 ≥ 0.72（低于阈值的结果不显示）"
    - "SSE 流式输出中换行符正确渲染为换行（而非显示 \\n 字符）"
    - "设置页可选 glm-5，AI 分析和工作台均使用"
    - "各 AI 模块使用差异化身份声明的 Prompt，含 Few-shot 正负例"
  artifacts:
    - path: "scripts/review_testcases.py"
      provides: "历史用例审查脚本（--dry-run 可验证）"
    - path: "backend/app/ai/prompts.py"
      provides: "6 模块重写后 Prompt"
    - path: "backend/app/engine/rag/retriever.py"
      provides: "retrieve_similar_cases 参数确认（top_k=5, score_threshold=0.72）"
    - path: "backend/app/modules/generation/router.py"
      provides: "/api/generation/rag-preview 端点，返回 score 字段"
  key_links:
    - from: "工作台 Step1 RAG 预览"
      to: "retrieve_similar_cases(top_k=5, score_threshold=0.72)"
      via: "RagPreviewPanel → /api/generation/rag-preview"
      pattern: "score_threshold.*0.72"
    - from: "backend/app/modules/generation/router.py"
      to: "backend/app/engine/rag/retriever.py"
      via: "retrieve_similar_cases 调用，显式传 top_k=5, score_threshold=0.72"
      pattern: "retrieve_similar_cases"
---

<objective>
Phase 3 终态验证：确认 RAG 检索参数正确（RAG-05），以及人工验证 SSE 换行和 GLM-5 切换在真实环境中工作。

Purpose: Plan 01 和 Plan 02 完成后，进行端到端功能确认，确保所有 Phase 3 成功标准可被验证。
Output: 若检索参数不对则修正 retriever.py / router.py，无其他新代码
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-01-SUMMARY.md
@.planning/phases/03-ai/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证并修正 RAG 检索参数（RAG-05）</name>
  <files>
    backend/app/engine/rag/retriever.py
    backend/app/modules/generation/router.py
  </files>
  <action>
    RAG-05 要求：retrieve_similar_cases top-5，阈值 0.72，检索结果注入 Prompt 时带相似度分数。

    验证步骤：
    1. 读取 backend/app/engine/rag/retriever.py 确认 retrieve_similar_cases 签名
       - top_k 默认值为 5 ✓
       - score_threshold 默认值为 0.72 ✓

    2. 读取 backend/app/modules/generation/router.py，找到 RAG 预览端点（/rag-preview）
       - 确认调用 retrieve_similar_cases 时传入 top_k=5, score_threshold=0.72
       - 确认返回结果包含 score 字段

    3. 若参数不对，修正调用处的参数（不修改 retriever.py 默认值，而是显式传参）
       - 若端点不存在，在 router.py 新增 GET /rag-preview?query=...&product=... 端点
       - 端点返回格式：{"results": [{"title": str, "content": str, "score": float}]}

    4. 运行已有 RAG 检索相关测试：
       `cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_rag/ -k "retrieve" -x -q`

    5. 运行全套测试确认无回归：
       `cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/ -q`
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/test_rag/ tests/unit/test_ai/test_prompts.py -x -q</automated>
  </verify>
  <done>
    所有单元测试通过。
    retrieve_similar_cases 的调用处显式指定 top_k=5, score_threshold=0.72（或确认默认值已是目标值）。
    检索结果的 score 字段在 API 响应中可见。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 3 端到端人工验收</name>
  <what-built>
    Plan 01: 历史用例 LLM 审查脚本（scripts/review_testcases.py）
    Plan 02: 5 个模块 Prompt 重写（Few-shot 正负例）+ SSE 换行修复 + GLM-5 配置验证
    Plan 03 Task 1: RAG 检索参数确认（top-5 / 0.72）
  </what-built>
  <how-to-verify>
    **验证 1：审查脚本（RAG-01~04, RAG-07, RAG-08）**
    ```bash
    cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform
    uv run python scripts/review_testcases.py --dry-run
    ```
    预期：打印找到的 CSV 文件数量（应接近 228）并正常退出，无报错。

    **验证 2：SSE 换行渲染（RAG-06）**
    1. 启动前端开发服务器（`cd frontend && bun dev`）
    2. 进入工作台（/workbench）
    3. 在 Step 1 确认至少一个测试点，点击「开始生成」
    4. 观察 Step 2 的 SSE 流式输出：步骤之间是否显示为换行（而非 `\n` 字符）
    预期：用例步骤应换行显示，每步独立一行。

    **验证 3：GLM-5 配置（PRM-04）**
    进入「设置」→「AI 配置」，查看模型选择器默认值是否为 glm-5。
    预期：默认显示 glm-5 或有可选项 glm-5。

    **验证 4：RAG 历史用例预览（RAG-05）**
    工作台 Step 1，勾选一个测试点，右侧 RAG 预览面板应显示相似历史用例（若向量库为空则显示空状态而非报错）。
    预期：面板正常渲染，每条结果含相似度分数（如 0.85）；空库时显示空状态提示。
  </how-to-verify>
  <action>人工按照 how-to-verify 中的 4 个验证步骤逐一确认</action>
  <verify><automated>MANUAL — 见 how-to-verify</automated></verify>
  <done>4 个验证点均通过，回复 "approved"</done>
  <resume-signal>
    确认通过：输入 "approved"
    发现问题：描述具体问题（如"Step 2 仍显示 \n 字符"、"dry-run 报 ImportError"）
  </resume-signal>
</task>

</tasks>

<verification>
所有单元测试通过：
```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/backend && uv run pytest tests/unit/ -q
```

人工验证通过（checkpoint approved）。
</verification>

<success_criteria>
- RAG-01~04: review_testcases.py --dry-run 无报错，找到 ~228 个 CSV 文件
- RAG-05: retrieve_similar_cases 调用处确认 top_k=5, score_threshold=0.72；router.py rag-preview 端点返回 score 字段
- RAG-06: 工作台 Step2 流式输出中 \\n 渲染为换行（人工确认）
- RAG-07: csv_fields 测试通过（BOM 解析正确，字段名中文）
- RAG-08: recreate 测试通过（collection 清空重建逻辑正确）
- PRM-01~03: prompts 测试全通过（四段式 + 身份差异化 + Few-shot）
- PRM-04: GLM-5 默认值确认，设置页可见（人工确认）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-ai/03-03-SUMMARY.md`，记录：
- 全部 Phase 3 需求验收结果
- RAG 检索参数的最终调用方式
- 人工验证结果（通过/问题）
- 如有未解决问题，记录到 STATE.md 的 Blockers 节
</output>
