---
phase: 02-main-refactor
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx
  - frontend/src/app/(main)/diagnosis/_components/AnalysisTab.tsx
  - frontend/src/app/(main)/diagnosis/_components/ScanResultsList.tsx
autonomous: true
requirements:
  - ANA-04
  - ANA-06
  - ANA-07

must_haves:
  truths:
    - "AI 分析 Tab 内，广度扫描结果区在上，苏格拉底追问对话框在下，两区同屏可见"
    - "上下两区之间有 4px 可拖拽分割线，拖拽可调节高度比例"
    - "高风险条目以内联展开卡片呈现，左边框颜色区分：critical=sy-danger / major=sy-warn / minor=sy-info"
    - "每条高风险项右侧有「确认知晓」按钮，点击后调用 PATCH confirm 端点，按钮变为「已确认」灰态"
    - "所有高风险项均确认后「进入工作台」按钮激活；仍有未确认时置灰，hover 显示提示文案"
    - "追问对话区流式输出期间输入框禁用，完成后恢复"
    - "分析结果刷新后从 DB 完整恢复（useDiagnosis hook 加载时恢复）"
  artifacts:
    - path: "frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx"
      provides: "增强版 AI 分析 Tab（含拖拽分割、风险确认、进入工作台置灰）"
      min_lines: 150
  key_links:
    - from: "frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx"
      to: "backend PATCH /diagnosis/risks/{id}/confirm"
      via: "fetch PATCH request on confirm button click"
      pattern: "confirm.*risk|risks.*confirm"
    - from: "进入工作台按钮"
      to: "/workbench?reqId={reqId}"
      via: "router.push with reqId query param"
      pattern: "reqId"
---

<objective>
增强 AI 分析 Tab：实现上下可拖拽分割布局、高风险条目内联展开卡片 + 确认知晓按钮、「进入工作台」置灰逻辑。

Purpose: ANA-04（上下布局）、ANA-07（置灰逻辑）的前端实现，依赖 Plan 01 提供的 PATCH confirm 端点。ANA-06（刷新恢复）通过复用 useDiagnosis hook 已有持久化能力实现。
Output: 增强版 AnalysisTab.tsx，移入 analysis/_components/ 目录。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-main-refactor/02-CONTEXT.md
@.planning/phases/02-main-refactor/02-RESEARCH.md

<interfaces>
<!-- 关键接口和现有实现，executor 直接使用 -->

DiagnosisRisk 接口（前端类型，注意：后端 model 字段名是 level，需确认前端 api.ts 中的序列化）：
```typescript
interface DiagnosisRisk {
  id: string;
  level: "high" | "medium" | "low";  // 使用 level 而非 severity（见 RESEARCH Pitfall 2）
  description: string;
  confirmed: boolean;  // Plan 01 新增字段
}
```

高风险置灰逻辑（CONTEXT.md 锁定）：
```typescript
const hasUnhandledHighRisk = (risks ?? []).some(
  (r) => r.level === 'high' && !r.confirmed
);
const unhandledCount = (risks ?? []).filter(
  (r) => r.level === 'high' && !r.confirmed
).length;

// hover tooltip 文案（固定文案，CONTEXT.md 锁定）：
`存在 ${unhandledCount} 个高风险遗漏项未确认，请先在 AI 分析 Tab 中处理`
```

跳转工作台参数（RESEARCH.md Pitfall 1 锁定）：
```typescript
// 使用 reqId 而非 requirementId
router.push(`/workbench?reqId=${selectedReqId}`)
```

拖拽分割线原生实现模式（RESEARCH.md Pattern 2）：
```typescript
// 上下分割：控制上区高度百分比（最小 30%，最大 70%）
const [splitRatio, setSplitRatio] = useState(0.5);
const handleSplitDrag = (e: React.MouseEvent) => {
  e.preventDefault();
  const container = containerRef.current;
  const startY = e.clientY;
  const startRatio = splitRatio;
  const onMove = (e: MouseEvent) => {
    const delta = e.clientY - startY;
    const containerHeight = container!.getBoundingClientRect().height;
    setSplitRatio(Math.min(0.7, Math.max(0.3, startRatio + delta / containerHeight)));
  };
  const onUp = () => {
    document.removeEventListener('mousemove', onMove);
    document.removeEventListener('mouseup', onUp);
  };
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup', onUp);
};
```

风险级别左边框颜色（CONTEXT.md 锁定）：
- critical → `border-l-4 border-sy-danger`
- major → `border-l-4 border-sy-warn`
- minor → `border-l-4 border-sy-info`

注意：现有 ScanResultsList.tsx 中可能用 `risk.severity`，实际后端字段是 `level`。如发现不一致，统一改为 `level`。

useDiagnosis hook（已存在）：
- 提供 `report`, `risks`, `messages` 等状态，加载时从 DB 恢复
- 提供 `sendMessage` 方法用于追问对话
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建增强版 AnalysisTab.tsx（上下拖拽分割 + 风险确认）</name>
  <files>
    frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx
  </files>
  <action>
    在 `analysis/_components/` 目录创建新的 `AnalysisTab.tsx`（独立于 diagnosis/_components/AnalysisTab.tsx，后者保留不动）。

    组件结构：
    ```tsx
    interface AnalysisTabProps {
      requirementId: string | null;
      visible: boolean;  // 防止 hidden 时触发请求（Pitfall 3）
    }
    ```

    1. **数据加载**：调用 `useDiagnosis(requirementId)`（已有 hook），`visible=false` 时跳过请求（在 hook 调用前检查 `if (!visible || !requirementId) return`）。

    2. **上方广度扫描结果区**（高度 `splitRatio * 100%`）：
       - 标题：「广度扫描结果」
       - 渲染 `report.risks` 列表，每条风险项显示为内联展开卡片：
         - 卡片左边框颜色：risk.level === "high" → `border-sy-danger`，"medium" → `border-sy-warn`，"low" → `border-sy-info`
         - 展开后显示完整描述
         - 右上角按钮：`confirmed=false` → 绿色「确认知晓」按钮；`confirmed=true` → 灰色「已确认」（disabled）
         - 点击「确认知晓」：调用 `PATCH /api/diagnosis/risks/{risk.id}/confirm`，成功后 optimistic update `risk.confirmed = true`，同时刷新 useDiagnosis 状态

    3. **分割线**（高度 4px，`cursor-row-resize`，hover 时 `bg-sy-accent`）：
       - 使用上方描述的拖拽实现（splitRatio 范围 0.3–0.7）

    4. **下方苏格拉底追问对话区**（高度 `(1-splitRatio) * 100%`）：
       - 渲染对话消息列表（`messages` 数组，role=user/assistant）
       - 底部 `ChatInput`（`isStreaming` 时禁用，与现有 workbench ChatInput 逻辑一致）
       - 发送消息调用 `sendMessage(content)`

    5. **右上角「进入工作台」按钮**（悬浮在广度扫描区右上方）：
       - `disabled={hasUnhandledHighRisk}` 且 `opacity-50 cursor-not-allowed`
       - 使用 title attribute 或 Tooltip 显示置灰文案（CONTEXT.md 锁定文案）
       - 未置灰时：`router.push('/workbench?reqId=' + requirementId)`（使用 `reqId` 参数，Pitfall 1）

    注意：置灰文案在 CONTEXT.md 中有明确锁定：`「存在 N 个高风险遗漏项未确认，请先在 AI 分析 Tab 中处理」`
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "analysis/_components/AnalysisTab" | head -10</automated>
  </verify>
  <done>
    - AnalysisTab.tsx 文件存在于 analysis/_components/
    - TypeScript 无编译错误
    - 组件接受 requirementId 和 visible props
  </done>
</task>

<task type="auto">
  <name>Task 2: 将增强版 AnalysisRightPanel 中 AnalysisTab import 指向新文件 + 修复 ScanResultsList 字段名</name>
  <files>
    frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx,
    frontend/src/app/(main)/diagnosis/_components/ScanResultsList.tsx
  </files>
  <action>
    1. 修改 `AnalysisRightPanel.tsx`（Plan 02 创建）中的 AnalysisTab import：
       将 `import AnalysisTab from "../../diagnosis/_components/AnalysisTab"` 改为
       `import AnalysisTab from "./AnalysisTab"`（使用本 plan 创建的增强版）。

    2. 检查 `diagnosis/_components/ScanResultsList.tsx` 中是否使用 `risk.severity`（前端字段名），
       而后端实际返回的是 `level`（RESEARCH.md Pitfall 2）。
       如果确认使用了 `severity` 但后端返回的是 `level`，则：
       - 统一改为 `risk.level`，同时更新对应的 TypeScript 接口（`DiagnosisRisk.severity` → `DiagnosisRisk.level`）
       - 更新 `frontend/src/lib/api.ts`（或相关类型文件）中的 `DiagnosisRisk` interface

    3. 确认 `AnalysisRightPanel.tsx` 中「进入工作台」占位按钮已替换为 AnalysisTab 内部的按钮（AnalysisTab 现在自己包含该按钮）；或在 AnalysisRightPanel 中删除占位按钮，避免重复。

    注意：若字段名确认一致（后端已序列化为 severity），则不需修改，保持现状。优先根据实际代码决定，不要盲目改变工作代码。
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -c "error" && echo "type check done"</automated>
  </verify>
  <done>
    - AnalysisRightPanel 使用新的 analysis/_components/AnalysisTab
    - DiagnosisRisk 字段名一致（前后端统一）
    - TypeScript 全局无新增错误
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend
bunx tsc --noEmit 2>&1 | tail -5
bunx biome check src/app/\(main\)/analysis/_components/ 2>&1 | tail -5
```
</verification>

<success_criteria>
- AI 分析 Tab 上下可拖拽分割（分割线 4px，拖拽范围 30%–70%）
- 高风险条目有「确认知晓」按钮，点击后变「已确认」灰态，调用 PATCH confirm 端点
- 「进入工作台」按钮：有未确认高风险时置灰，hover 文案正确；全部确认后激活，点击跳转 /workbench?reqId=xxx
- 跳转使用 reqId 参数（不是 requirementId）
- TypeScript 和 Biome 无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/02-main-refactor/02-03-SUMMARY.md`
</output>
