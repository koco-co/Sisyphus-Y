---
phase: 02-main-refactor
plan: "06"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - frontend/src/app/(main)/analysis/page.tsx
  - frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx
  - frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx
  - frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx
  - frontend/src/app/(main)/settings/page.tsx
  - backend/app/modules/diagnosis/router.py
autonomous: true
requirements:
  - ANA-01

must_haves:
  truths:
    - "分析台所有可见文案中「诊断」已替换为「分析」（按钮、Tab 标签、标题、空状态文案）"
    - "导航栏菜单项显示「分析台」（原「诊断」）"
    - "设置页操作日志中「诊断」相关文案已更新"
    - "后端 API 路径 /api/diagnosis/... 保持不变（不做破坏性路由变更）"
    - "前端 API 调用路径不变（仍调用 /api/diagnosis/... 后端端点）"
  artifacts:
    - path: "frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx"
      provides: "所有用户可见文案使用「分析」而非「诊断」"
    - path: "frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx"
      provides: "Tab 标签显示「需求详情 | AI 分析 | 覆盖追踪」"
  key_links:
    - from: "导航栏菜单项"
      to: "/analysis 路由"
      via: "文案「分析台」"
      pattern: "分析台"
---

<objective>
全局「诊断」→「分析」文案替换（ANA-01），完成分析台最终打磨：核对所有用户可见文案，更新导航栏，确保 Phase 2 分析台交付的完整性。

Purpose: Phase 2 的收尾 plan，与 Plan 05 并行执行（无文件冲突）。后端 API 路径 /api/diagnosis 保持不变（避免破坏性变更），仅更新前端文案和组件注释。
Output: 全局文案统一，导航栏、分析台、设置页中「诊断」字样全部替换。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-main-refactor/02-CONTEXT.md
@.planning/phases/02-main-refactor/02-RESEARCH.md
@.planning/phases/02-main-refactor/02-02-SUMMARY.md
@.planning/phases/02-main-refactor/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端全局「诊断」→「分析」文案扫描与替换</name>
  <files>
    frontend/src/app/(main)/analysis/page.tsx,
    frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx,
    frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx,
    frontend/src/app/(main)/analysis/_components/AnalysisTab.tsx,
    frontend/src/app/(main)/layout.tsx（导航栏菜单项）
  </files>
  <action>
    1. **搜索范围**：用 grep 扫描 `frontend/src/` 下所有 tsx/ts 文件中用户可见文案里的「诊断」：
       `grep -r "诊断" frontend/src/ --include="*.tsx" --include="*.ts" -l`

       重点文件：
       - 导航栏（`(main)/layout.tsx` 或 `_components/` 中的 TopBar 组件）
       - analysis/_components/ 下所有文件
       - 设置页（settings/page.tsx）
       - 原 diagnosis/_components/（保留现有文件但不强制修改，避免破坏现有诊断页面入口）

    2. **替换规则**（仅用户可见文案，不修改 import 路径、变量名、函数名）：
       - 「健康诊断」→「需求分析」（如有）
       - 「诊断分析」→「AI 分析」（如有）
       - 「诊断报告」→「分析报告」（如有）
       - 「诊断」（单独出现作为页面标题或按钮文案）→「分析」
       - Tab 标签「AI 分析」保持不变（已经是正确文案）
       - Tab 标签「需求详情」保持不变
       - Tab 标签「覆盖追踪」保持不变

    3. **导航栏菜单项**：找到导航栏中「分析台」或「诊断」对应的菜单项，确认显示「分析台」（若已是正确文案则无需改动）。

    4. **不修改的内容**：
       - TypeScript 变量名：`diagnosisStore`、`useDiagnosis`、`DiagnosisRisk` 等（保持 camelCase 命名）
       - API 路径字符串：`/api/diagnosis/...`（后端路由不变）
       - 文件名和 import 路径
       - diagnosis/ 目录下的组件（保留兼容入口）

    5. 完成后再次 grep 确认分析台相关文件中无孤立「诊断」文案残留。
  </action>
  <verify>
    <automated>grep -r "诊断" /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/app/\(main\)/analysis/ --include="*.tsx" | grep -v "//.*诊断" | wc -l</automated>
  </verify>
  <done>
    - analysis/_components/ 下所有文件中用户可见「诊断」文案已替换为「分析」
    - 导航栏「分析台」文案正确
    - grep 结果：analysis/ 目录下可见「诊断」字符数为 0（注释中可保留）
  </done>
</task>

<task type="auto">
  <name>Task 2: 分析台收尾——空状态页、AiConfigBanner、TypeScript 最终检查</name>
  <files>
    frontend/src/app/(main)/analysis/page.tsx,
    frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx
  </files>
  <action>
    1. **空状态页面**（Claude's Discretion）：
       - `AnalysisLeftPanel` 无需求时：显示 `FileText` 图标（48px, `text-sy-text-3`）+ 「暂无需求，请先在需求管理中添加需求」+ 「前往添加」按钮（跳转 `/requirements`）
       - `AnalysisRightPanel` 未选中需求时：显示 `MousePointerClick` 图标（48px）+ 「请从左侧列表选择一条需求」

    2. **AiConfigBanner**（复用 `components/ui/AiConfigBanner.tsx`）：
       在 `analysis/page.tsx` 顶部引入 `<AiConfigBanner />`，检测 AI 未配置时显示警告横幅（组件已存在，直接复用）。

    3. **后端路由注释更新（可选低优先级）**：
       `backend/app/modules/diagnosis/router.py` 中 docstring 和注释里的「诊断」→「分析」，不影响 API 路径。
       仅修改注释内容，不修改路由路径和函数名。

    4. **最终 TypeScript 全量检查**：
       ```bash
       cd frontend && bunx tsc --noEmit
       ```
       修复所有因 Plan 02~06 引入的类型错误。

    5. **Biome lint 检查**：
       ```bash
       bunx biome check --write src/app/\(main\)/analysis/ src/app/\(main\)/workbench/
       ```
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    - 空状态页面有图标 + 文案 + 引导操作
    - AiConfigBanner 在分析台顶部挂载
    - TypeScript 全量检查无错误（或错误数不因本 Phase 引入新增）
    - Biome 无 error 级别警告
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    分析台完整体验：需求列表 + 三 Tab 状态保持 + AI 分析 Tab 布局 + 高风险置灰 + 「诊断」→「分析」文案统一
  </what-built>
  <how-to-verify>
    1. 访问 http://localhost:3000/analysis
    2. 验证：页面不跳转到 /requirements，渲染独立三栏布局
    3. 验证：左栏需求列表可折叠迭代分组，搜索框过滤需求标题
    4. 验证：拖拽左栏右边缘，宽度在 200–320px 范围内变化
    5. 选中一条需求，切换三个 Tab（需求详情 / AI 分析 / 覆盖追踪），确认切换无状态丢失
    6. 刷新页面，确认 Tab 和选中需求状态从 URL/localStorage 恢复
    7. 在 AI 分析 Tab 验证：上方广度扫描区 + 下方追问对话区，拖拽中间分割线可调节高度比例
    8. 验证：页面上无「诊断」字样（按钮/标题/Tab 标签），均已更新为「分析」
    9. 验证：导航栏显示「分析台」
    10. 如有高风险项：验证置灰按钮和 hover 文案正确；点击「确认知晓」后按钮变灰态
  </how-to-verify>
  <resume-signal>输入 "approved" 确认分析台验收通过，或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
```bash
# 确认 analysis/ 目录下无孤立「诊断」文案
grep -r "诊断" /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend/src/app/\(main\)/analysis/ --include="*.tsx" | grep -v "//"

# 全量 TypeScript 检查
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | tail -10
```
</verification>

<success_criteria>
- 分析台所有可见文案：「诊断」→「分析」，导航栏「分析台」正确
- 空状态页面有图标 + 文案 + 操作按钮
- AiConfigBanner 在分析台顶部
- TypeScript 全量检查无错误（Phase 2 引入部分）
- 人工验收通过（checkpoint 确认）
</success_criteria>

<output>
完成后创建 `.planning/phases/02-main-refactor/02-06-SUMMARY.md`
</output>
