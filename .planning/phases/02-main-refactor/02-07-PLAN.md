---
phase: 02-main-refactor
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(main)/workbench/page.tsx
autonomous: true
gap_closure: true
requirements:
  - WRK-02

must_haves:
  truths:
    - "工作台 Step1 中栏测试点列表按场景类型分组展示，每组超过5条时显示「展示更多 (N)」按钮，点击展开/收起"
    - "每组末尾有「添加测试点」行，新增条目自动归属该组"
    - "勾选测试点后「开始生成/追加生成」按钮激活并触发生成流程"
  artifacts:
    - path: "frontend/src/app/(main)/workbench/page.tsx"
      provides: "使用 TestPointGroupList 替代 TestPointList 的工作台中栏"
      contains: "TestPointGroupList"
  key_links:
    - from: "workbench/page.tsx (Step1 中栏)"
      to: "workbench/_components/TestPointGroupList.tsx"
      via: "import + JSX 替换"
      pattern: "TestPointGroupList"
---

<objective>
将工作台 Step1 中栏的 TestPointList 替换为 TestPointGroupList，激活 WRK-02 要求的「超5条展示更多/收起」折叠逻辑。

Purpose: TestPointGroupList 已实现完整的 FOLD_THRESHOLD=5 折叠能力和「每组末尾添加测试点」行，但因 workbench/page.tsx 仍使用旧的 TestPointList 而成为孤儿组件，导致 WRK-02 要求未生效。
Output: workbench/page.tsx 中栏切换为 TestPointGroupList，折叠行为上线。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-main-refactor/02-CONTEXT.md
</context>

<interfaces>
<!-- TestPointGroupList props interface (from workbench/_components/TestPointGroupList.tsx) -->
```typescript
interface TestPointGroupListProps {
  requirementId: string | null;
  checkedPointIds: Set<string>;
  onToggle: (id: string) => void;
  onAdd: (groupName: string, title: string) => void;
  onStartGenerate: () => void;
}
```

<!-- TestPointGroupList 内部调用 useSceneMap() 自行获取 testPoints，不需要外部传入。
     底部内置「开始生成（N 个测试点）」按钮，通过 onStartGenerate 回调触发外部逻辑。
     disabled 状态由内部 checkedPointIds.size === 0 控制。
-->

<!-- workbench/page.tsx 当前 Step1 中栏（第280-293行）: -->
```tsx
<div className="min-h-0 flex-1 overflow-hidden bg-sy-bg">
  <TestPointList
    testPoints={sm.testPoints}
    selectedPointId={sm.selectedPointId}
    checkedPointIds={sm.checkedPointIds}
    searchQuery={sm.searchQuery}
    isLocked={false}
    stats={sm.stats}
    onSelectPoint={sm.selectPoint}
    onToggleCheck={sm.toggleCheckPoint}
    onSearchChange={sm.setSearchQuery}
    onAddPoint={sm.addPoint}
  />
</div>
```

<!-- sm.addPoint 的签名（scene-map store）: addPoint(title: string, groupName?: string) -->
<!-- sm.toggleCheckPoint 的签名: toggleCheckPoint(id: string) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 替换 TestPointList 为 TestPointGroupList</name>
  <files>frontend/src/app/(main)/workbench/page.tsx</files>
  <action>
在 workbench/page.tsx 中完成以下修改：

**Step A — 更新 import**

移除：
```typescript
import { TestPointList } from '../scene-map/_components/TestPointList';
```

添加：
```typescript
import TestPointGroupList from './_components/TestPointGroupList';
```

**Step B — 替换 Step1 中栏 JSX（第280-293行附近）**

将当前：
```tsx
<div className="min-h-0 flex-1 overflow-hidden bg-sy-bg">
  <TestPointList
    testPoints={sm.testPoints}
    selectedPointId={sm.selectedPointId}
    checkedPointIds={sm.checkedPointIds}
    searchQuery={sm.searchQuery}
    isLocked={false}
    stats={sm.stats}
    onSelectPoint={sm.selectPoint}
    onToggleCheck={sm.toggleCheckPoint}
    onSearchChange={sm.setSearchQuery}
    onAddPoint={sm.addPoint}
  />
</div>
```

替换为：
```tsx
<div className="min-h-0 flex-1 overflow-hidden bg-sy-bg">
  <TestPointGroupList
    requirementId={wb.selectedReqId}
    checkedPointIds={sm.checkedPointIds}
    onToggle={sm.toggleCheckPoint}
    onAdd={(groupName, title) => sm.addPoint(title, groupName)}
    onStartGenerate={handleStartGeneration}
  />
</div>
```

**Step C — 移除 page.tsx header 中的重复「开始生成/追加生成」按钮**

TestPointGroupList 底部已内置生成按钮，page.tsx Step1 header 中的同功能按钮（第267-276行，含 `{store.lastGeneratedPointIds.size > 0 ? '追加生成' : '开始生成'}` 的按钮）与之重复。移除 page.tsx header 中这个按钮（保留「AI 生成测试点」按钮）。

注意：移除按钮后 `selectedPointCount` 变量可能变为未使用，检查是否还被其他地方引用（第43行定义），若已无其他用途则同时移除该变量声明以避免 Biome lint 报错。

**保留不变：**
- handleStartGeneration 函数（被 onStartGenerate 回调使用）
- sm.checkedPointIds（被 TestPointGroupList 使用）
- Step1 header 中的「AI 生成测试点」按钮和所有其他内容
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx biome check src/app/\(main\)/workbench/page.tsx && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - workbench/page.tsx 中不再有 TestPointList 引用
    - TestPointGroupList 已连入 Step1 中栏，props 正确传入（requirementId/checkedPointIds/onToggle/onAdd/onStartGenerate）
    - Biome lint 和 TypeScript 类型检查均通过，无新报错
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend
bunx biome check src/app/\(main\)/workbench/page.tsx
bunx tsc --noEmit 2>&1 | grep -E "workbench|TestPoint" | head -20
grep -n "TestPointGroupList" src/app/\(main\)/workbench/page.tsx
grep -n "TestPointList" src/app/\(main\)/workbench/page.tsx
```

预期：
- `TestPointGroupList` 出现在 import 行和 JSX 中
- `TestPointList` 不再出现（import 和 JSX 均已移除）
- Biome 和 TypeScript 检查无报错
</verification>

<success_criteria>
WRK-02 gap 已关闭：工作台 Step1 中栏由 TestPointGroupList 提供，超过5条测试点的分组会显示「展示更多 (N)」折叠按钮，每组末尾有「添加测试点」内联行。Biome + TypeScript 检查通过。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-main-refactor/02-07-SUMMARY.md`
</output>
