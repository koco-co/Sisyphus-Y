---
phase: 02-main-refactor
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/app/(main)/analysis/page.tsx
  - frontend/src/app/(main)/analysis/layout.tsx
  - frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx
  - frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx
autonomous: true
requirements:
  - ANA-02
  - ANA-03
  - ANA-06

must_haves:
  truths:
    - "/analysis 页面渲染独立三栏布局（不再 re-export /requirements）"
    - "左栏显示需求列表，按迭代分组，分组头默认折叠"
    - "每条需求显示状态 Badge 和高风险红色数字"
    - "左栏有搜索框，输入可过滤需求标题"
    - "左栏可拖拽调宽，范围 200–320px"
    - "右栏三 Tab（需求详情/AI 分析/覆盖追踪）常驻 DOM，切换无状态丢失"
    - "刷新页面后选中的需求和 Tab 状态完整恢复（从 URL 或 localStorage）"
  artifacts:
    - path: "frontend/src/app/(main)/analysis/page.tsx"
      provides: "分析台主页，三栏容器"
      min_lines: 60
    - path: "frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx"
      provides: "需求列表，迭代分组，搜索，拖拽调宽"
      min_lines: 100
    - path: "frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx"
      provides: "三 Tab 常驻 DOM 容器"
      min_lines: 60
  key_links:
    - from: "frontend/src/app/(main)/analysis/page.tsx"
      to: "frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx"
      via: "onSelectRequirement prop"
      pattern: "onSelectRequirement"
    - from: "frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx"
      to: "useRequirementTree hook"
      via: "import useRequirementTree"
      pattern: "useRequirementTree"
---

<objective>
分析台三栏布局骨架：重建 /analysis 为独立页面，实现左侧需求列表（带迭代分组、搜索、拖拽调宽）和右侧三 Tab 常驻 DOM 容器。

Purpose: 当前 /analysis/page.tsx 是 re-export，无法承载三栏布局。本 plan 建立正确的页面骨架，后续 plan 填充 Tab 内容。
Output: AnalysisLeftPanel.tsx、AnalysisRightPanel.tsx、重建后的 analysis/page.tsx。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-main-refactor/02-CONTEXT.md
@.planning/phases/02-main-refactor/02-RESEARCH.md

<interfaces>
<!-- 可复用的现有 hooks 和组件，executor 直接使用，无需探索 -->

从 frontend/src/app/(main)/diagnosis/page.tsx 提取的三 Tab 常驻 DOM 模式：
```tsx
// 三 Tab 常驻 DOM — 直接复用此模式
<div className="flex-1 overflow-hidden relative">
  <div className={`absolute inset-0 ${activeTab === 'detail' ? '' : 'hidden'}`}>
    <RequirementDetailTab />
  </div>
  <div className={`absolute inset-0 ${activeTab === 'analysis' ? '' : 'hidden'}`}>
    <AnalysisTab />
  </div>
  <div className={`absolute inset-0 ${activeTab === 'coverage' ? '' : 'hidden'}`}>
    <CoverageMatrix />
  </div>
</div>
```

useRequirementTree hook（已存在，直接 import）：
- 返回 `{ products, loading, toggleProduct, toggleIteration }`
- 三级树：product → iteration → requirements[]

StatusBadge 组件（frontend/src/components/ui/StatusBadge.tsx）：
- Props: `status: "pending" | "processing" | "completed" | "error"`
- 对应需求状态：未分析=pending / 分析中=processing / 已完成=completed

现有 diagnosis/page.tsx _components 可复用：
- RequirementDetailTab.tsx — 需求详情 Tab 内容（直接复用）
- AnalysisTab.tsx — AI 分析 Tab 内容（Plan 03 增强）
- CoverageMatrix.tsx — 覆盖追踪 Tab 内容（Plan 04 增强）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重建 analysis/page.tsx 为三栏布局容器</name>
  <files>
    frontend/src/app/(main)/analysis/page.tsx,
    frontend/src/app/(main)/analysis/layout.tsx
  </files>
  <action>
    1. 修改 `analysis/layout.tsx`：移除原有的子路由 Tab 导航（如果存在「需求列表 / 分析 / 覆盖追踪」这样的路由导航），保留页面标题区域，不再渲染 Tab 导航栏（Tab 切换移入右栏内部）。

    2. 重写 `analysis/page.tsx`（原文件是 re-export，完全替换）：
       - 使用 `ThreeColLayout` 组件（`components/layout/ThreeColLayout.tsx`），三栏独立滚动。
       - 使用 `useState` 管理：`selectedReqId: string | null`、`activeTab: "detail" | "analysis" | "coverage"`。
       - URL 参数读取：页面加载时从 `searchParams` 读取 `reqId` 初始化 `selectedReqId`；`activeTab` 初始默认 "detail"。
       - 渲染结构：
         ```tsx
         <ThreeColLayout
           left={<AnalysisLeftPanel ... />}
           center={<AnalysisRightPanel ... />}
           right={null}  // 右栏在 AnalysisRightPanel 内部管理
         />
         ```
       - 实际上分析台是「左栏 + 右栏」两栏（不是三栏），使用 flex row，左栏固定宽度（由 AnalysisLeftPanel 内部拖拽控制），右栏 flex-1。整体高度 `calc(100vh - 49px)`。

    注意：layout.tsx 如果只是简单 `export default function Layout({ children }) { return children }` 则不需要修改，检查后决定是否需要改动。
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "(analysis/page|analysis/layout)" | head -20</automated>
  </verify>
  <done>
    - /analysis 路由不再 re-export /requirements
    - page.tsx 导出独立的三栏结构组件
    - TypeScript 无编译错误
  </done>
</task>

<task type="auto">
  <name>Task 2: AnalysisLeftPanel — 需求列表、迭代分组、搜索、拖拽调宽</name>
  <files>
    frontend/src/app/(main)/analysis/_components/AnalysisLeftPanel.tsx
  </files>
  <action>
    创建 `AnalysisLeftPanel.tsx`，实现以下功能：

    1. **拖拽调宽**：`panelWidth` state 默认 260px，范围 200–320px。拖拽手柄：绝对定位于右边缘，宽度 4px，`hover:bg-sy-accent` 颜色，`cursor-col-resize`。使用研究文档中的 onMouseDown + mousemove 原生实现（无需新依赖）。

    2. **搜索框**：头部 `<input>` 搜索需求标题，`searchQuery` state，过滤所有迭代内的需求（仅标题匹配，大小写不敏感）。使用 `Search` 图标（lucide-react）。

    3. **迭代分组列表**：调用 `useRequirementTree` hook 获取数据。
       - 每个 product 下按 iteration 分组，`expandedIterations: Set<string>` state 控制折叠（默认全部折叠，第一个 iteration 默认展开作为当前迭代）。
       - 分组头：`[iteration.name] [需求数]`，点击 toggle，使用 `ChevronRight`/`ChevronDown` 图标。
       - 需求条目（展开时显示）：
         - 需求标题（截断 30 字）
         - `StatusBadge` 显示分析状态
         - 若 `unconfirmed_high_risk_count > 0`：右侧红色数字 badge（`bg-sy-danger text-white text-xs rounded-full px-1.5`）
         - 点击选中：高亮 `bg-sy-bg-2`，调用 `onSelectRequirement(reqId)`

    4. **Props**：
       ```tsx
       interface AnalysisLeftPanelProps {
         selectedReqId: string | null;
         onSelectRequirement: (reqId: string) => void;
       }
       ```

    5. **样式**：整体 `bg-sy-bg-1 border-r border-sy-border`，高度 `calc(100vh - 49px)`，`overflow-y-auto`。

    注意：需求数据中若无 `unconfirmed_high_risk_count` 字段（取决于 API 返回结构），则先跳过红色数字，仅展示 StatusBadge，Phase 2 Plan 03 会处理高风险计数显示。
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "AnalysisLeftPanel" | head -10</automated>
  </verify>
  <done>
    - AnalysisLeftPanel 文件存在，无 TypeScript 错误
    - 拖拽手柄 4px 宽度，hover 变 sy-accent 色
    - 搜索框过滤需求标题
    - 迭代分组头默认折叠，点击展开
    - 需求条目显示 StatusBadge
  </done>
</task>

<task type="auto">
  <name>Task 3: AnalysisRightPanel — 三 Tab 常驻 DOM 容器</name>
  <files>
    frontend/src/app/(main)/analysis/_components/AnalysisRightPanel.tsx
  </files>
  <action>
    创建 `AnalysisRightPanel.tsx`，实现三 Tab 常驻 DOM 容器：

    1. **Tab 导航栏**：水平 Tab 条（`需求详情 | AI 分析 | 覆盖追踪`），使用 `activeTab` state 控制激活态。Tab 按钮样式：激活时 `border-b-2 border-sy-accent text-sy-text`，未激活 `text-sy-text-2 hover:text-sy-text`。

    2. **三 Tab 常驻 DOM 模式**（复用 diagnosis/page.tsx 现有实现）：
       ```tsx
       <div className="flex-1 overflow-hidden relative">
         <div className={`absolute inset-0 ${activeTab === 'detail' ? '' : 'hidden'}`}>
           <RequirementDetailTab requirementId={selectedReqId} />
         </div>
         <div className={`absolute inset-0 ${activeTab === 'analysis' ? '' : 'hidden'}`}>
           <AnalysisTab requirementId={selectedReqId} visible={activeTab === 'analysis'} />
         </div>
         <div className={`absolute inset-0 ${activeTab === 'coverage' ? '' : 'hidden'}`}>
           <CoverageMatrix requirementId={selectedReqId} visible={activeTab === 'coverage'} />
         </div>
       </div>
       ```
       注意：传入 `visible` prop 给 AnalysisTab 和 CoverageMatrix，防止 hidden 状态下触发不必要 API 请求（Pitfall 3）。

    3. **右上角「进入工作台」按钮**：独立实现留给 Plan 03（ANA-07），此处先渲染占位 `<button disabled>进入工作台</button>`，后续 Plan 03 补充置灰逻辑。

    4. **Props**：
       ```tsx
       interface AnalysisRightPanelProps {
         selectedReqId: string | null;
         activeTab: "detail" | "analysis" | "coverage";
         onTabChange: (tab: "detail" | "analysis" | "coverage") => void;
       }
       ```

    5. **复用组件 import 路径**：
       - `RequirementDetailTab` from `../../diagnosis/_components/RequirementDetailTab`（暂时从 diagnosis 目录 import，后续可迁移）
       - `AnalysisTab` from `../../diagnosis/_components/AnalysisTab`
       - `CoverageMatrix` from `../../diagnosis/_components/CoverageMatrix`
  </action>
  <verify>
    <automated>cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend && bunx tsc --noEmit 2>&1 | grep -E "AnalysisRightPanel|analysis/_components" | head -10</automated>
  </verify>
  <done>
    - AnalysisRightPanel 文件存在，三 Tab 常驻 DOM 实现完整
    - 切换 Tab 时不卸载其他 Tab 的 DOM
    - TypeScript 无编译错误
    - visible prop 传入 AnalysisTab 和 CoverageMatrix
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/aa/WorkSpace/Projects/Sisyphus-case-platform/frontend
bunx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"
bunx biome check src/app/\(main\)/analysis/ 2>&1 | tail -5
```
</verification>

<success_criteria>
- /analysis 路由渲染独立三栏布局（浏览器访问 /analysis 不再跳转 /requirements）
- 左栏需求列表可折叠迭代分组、可搜索过滤、可拖拽调宽
- 右栏三 Tab 切换不卸载 DOM，刷新后 reqId 从 URL 参数恢复
- TypeScript 和 Biome 检查无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/02-main-refactor/02-02-SUMMARY.md`
</output>
